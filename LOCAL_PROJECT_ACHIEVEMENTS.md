# 无禁手五子棋形式化项目：三层次成果总览

> 本文档只作为本地学习资料，不需要上传 GitHub。
>
> 当前最重要的事实：我们已经把规则、若干局部获胜情形、完整博弈含义以及一种可检查的获胜说明书做出来了；但是还没有得到从 15×15 空棋盘开始的完整黑棋必胜说明书，所以还没有证明“黑棋从开局必胜”。

## 如何阅读本文

本文把同一项目讲三遍，读者可以按自己的背景选择。

1. 第一部分完全不用代码和证明软件的专门词汇，回答“我们到底做了什么”。
2. 第二部分面向熟悉数学、程序验证或五子棋研究的人，给出准确的专业摘要。
3. 第三部分逐层说明每个部分如何表示、如何连接、证明了什么，以及还有什么没有证明。

---

# 第一部分：完全不懂编程的人也能看懂的介绍

## 1. 这个项目想解决什么问题

我们最终想回答一个很强的问题：

> 在 15×15 的无禁手五子棋中，如果双方都采用最好的下法，先下的黑棋是否一定能赢？

只说“大家都知道黑棋有优势”不够，只给出一盘黑棋获胜的棋谱也不够。因为白棋在每一步都可能有许多不同回应。若要证明黑棋一定能赢，就必须说明：无论白棋每次怎样合法应对，黑棋总有下一步办法，最后都会走到黑棋五子相连。

这相当于写一本极其庞大的“应对手册”：

- 黑棋先走哪一步；
- 白棋如果这样走，黑棋怎样应对；
- 白棋如果换一种走法，黑棋又怎样应对；
- 每一种白棋可能采取的合法走法都不能遗漏；
- 每条分支的最后都必须确实是黑棋获胜，不能是白棋获胜或和棋。

我们的工作就是先把“什么叫规则正确”“什么叫黑棋无论如何都能赢”“怎样检查这样一本应对手册”说得毫无歧义，然后再逐步寻找这本完整手册。

## 2. 为什么不能直接从“看起来必胜”跳到结论

人下五子棋时，经常会说：

- “这里有一个活四，肯定赢了。”
- “这里有双活三，对方堵不住。”
- “这个局面黑棋明显占优。”

这些说法在日常下棋时可能够用，但严密证明要求更高。比如：

- 现在到底轮到谁？
- 想落子的地方是不是空的？
- 棋局是不是已经结束了？
- 所谓“双活三”具体包括哪些形状？
- 两个形状会不会其实是在重复数同一条线？
- 白棋堵住一个威胁时，会不会顺便形成自己的四连？
- 白棋是否已经有下一步立即获胜的位置？
- 黑棋说“仍然能赢”时，是否真的检查了白棋所有可能应对？

我们的做法是把这些隐藏条件全部拆开，一个一个写清楚。只要有一个条件没有被证明，就不把它叫作已经证明的必胜。

## 3. 我们已经把五子棋规则写清楚了

我们已经让计算机能够严格区分并检查以下事情：

- 棋盘固定为 15 行、15 列；
- 棋盘上的一个位置只能是空、黑棋或白棋；
- 黑棋先下；
- 黑白双方轮流下；
- 只能下在空位置；
- 横向、纵向、左斜和右斜都可以形成五连；
- 连续五个或更多同色棋子就是胜利，因此六连也算赢；
- 一方形成五连后棋局立刻结束；
- 棋局结束后不能再继续下；
- 棋盘填满但没有任何一方五连时是和棋；
- 不采用三三禁手、四四禁手或长连禁手。

我们还证明了规则执行过程中不会出现一些不正常现象：

- 黑棋回合时，黑白棋子数量相同；
- 白棋回合时，黑棋恰好多一颗；
- 每下一步，空位置恰好少一个；
- 已经放好的其他棋子不会被合法落子改变；
- 从空棋盘正常走出来的棋局不会出现黑白双方同时已经获胜。

## 4. 我们已经能严格识别一些棋形

我们已经让计算机识别：

- 一条线上连续的三颗棋子，两端都为空；
- 一条线上连续的四颗棋子，两端都为空；
- 中间缺一格的三子形状；
- 五格范围内有四颗同色棋子、中间缺一格的形状；
- 下一步落下就能形成五连的位置；
- 同时存在两个不同的下一步获胜位置。

棋盘边缘不会被误当作空位置。例如三个棋子靠着边界，只有另一端是空的，就不能被当成“两端都能继续”的三连。

我们还防止了重复计数：同一条连续棋子不能只因为选择了不同的起点就被误认为是两条不同的棋形。

## 5. 我们已经证明了一些简单而真实的必胜情形

目前已经严格证明的典型情形包括：

- 黑棋已经有连续四子且两端为空，轮到黑棋时，黑棋可以下一步形成五连；
- 在五个连续位置中已经有四颗黑棋、只缺中间一格时，填上缺口可以立即获胜；
- 如果黑棋有两个不同的“下一步落下就赢”的位置，那么白棋一次只能占掉其中一个，黑棋仍可以在另一个位置获胜；
- 某些特别小、所有空位都能逐一列出的局面，可以把白棋每一种应对都列出来并证明黑棋获胜。

但我们没有证明“只要看起来像双活三就一定获胜”。我们实际构造过反例来提醒自己：棋形漂亮不等于所有后续都已经得到证明。

## 6. 我们定义了“无论对方怎么走都能赢”

“能赢”不能只指存在一条顺利的棋谱。真正的意思是：

- 如果轮到想获胜的一方，他至少有一种合法选择能够保住胜势；
- 如果轮到对方，那么对方的每一种合法选择之后，胜势都必须仍然存在；
- 最后必须真的到达目标一方获胜，而不能把和棋或对方获胜忽略掉。

我们已经把这套意思写成可以反复使用的严格定义。局部棋形证明和未来的全局证明都使用同一个“必胜”标准，不会各说各话。

## 7. 我们做了一种“获胜说明书”和核验工具

完整开局的所有变化数量太大，靠人在文件里直接写完整证明不现实。因此我们把工作分成两类：

1. 一个寻找答案的程序负责尝试找出黑棋怎样应对。
2. 一个更小、更严格的核验工具负责检查它交来的说明书。

核验工具不相信寻找程序。它会重新检查：

- 每一步是不是合法；
- 轮到黑棋时，说明书是否给出一个选择；
- 轮到白棋时，说明书是否列全了白棋所有合法选择；
- 每条连接前后的棋盘是否真的对应那一步落子；
- 最后的胜负是否写对；
- 有没有引用不存在的内容；
- 有没有绕圈子，假装证明却永远不到终点；
- 整本说明书是不是确实从空棋盘、黑棋先手开始。

我们已经证明：如果一本说明书通过这个核验工具，那么它确实说明黑棋从空棋盘能够强制获胜。

## 8. 我们还主动做了“挑错测试”

我们没有只测试正确例子，还故意制造错误内容，例如：

- 在已经有棋子的地方落子；
- 把棋盘边界当成空位；
- 漏掉白棋的一种合法防守；
- 写错下一步棋盘；
- 写错最后的胜者；
- 引用不存在的后续内容；
- 让说明书形成循环；
- 用错误的开局作为整本说明书的开头。

核验工具会拒绝这些错误内容。这样的工作不能保证永远没有错误，但它比只验证几个正面例子更能暴露定义中的漏洞。

## 9. 我们为以后寻找完整答案做了哪些准备

已经有一个初步的自动寻找工具，它会：

- 列出棋盘上所有可以落子的地方；
- 优先尝试附近有棋子的地方；
- 优先尝试下一步直接获胜的位置；
- 识别对方下一步可能获胜、需要防守的位置；
- 在很浅的步数范围内尝试所有需要覆盖的变化；
- 把找到的结果转换成核验工具能够读取的说明书；
- 对小型局面完成从“寻找”到“检查”再到“确认获胜”的全过程。

我们还做了更快的五连检查：新落一颗棋后，只检查经过这颗棋的相关位置，而不必每次从头检查整张棋盘。这个快速方法和完整检查之间的一致性已经得到证明。

我们也为重复局面准备了精确编号方法：编号保存当前轮到谁和全部 225 个位置，不会把两个不同棋盘误认为同一棋盘。目前这只是以后加速所需的基础，还没有真正让深层寻找过程利用它。

## 10. 现在最重要的未完成部分

我们还没有得到那本覆盖 15×15 空棋盘全部变化的完整黑棋获胜说明书。

因此，当前正确结论是：

> 我们已经建好了规则、局部证明方法、完整的“必胜”含义、说明书格式和可信核验工具，并在小局面上验证了整个流程。

当前不能说：

> 我们已经证明无禁手五子棋黑棋从空棋盘必胜。

剩下最困难的工作不是再解释一次规则，而是大幅提高寻找工具的能力，得到足够大且完整的说明书，再交给已经完成的核验工具确认。

---

# 第二部分：面向内行的专业摘要

## 11. 项目范围与可信边界

项目使用 Lean 4.33.0 与 mathlib v4.33.0，在 `Gomoku` 命名空间内形式化固定 15×15 无禁手五子棋。规则采用 freestyle Gomoku 语义：黑先、交替落子、至少五连即胜、长连计胜、成五立即终局、满盘无胜为和棋。

最终目标为：

```lean
theorem initial_black_wins :
  CanForceWin initialPosition .black
```

该定理当前尚未声明。现有可信链闭合到了：

```lean
theorem compact_certificate_sound (c : CompactCertificate)
    (h : checkCertificate c = true) :
    CanForceWin initialPosition .black
```

因此全局结论只缺一个实际通过 `checkCertificate` 的 15×15 策略证书；搜索器、启发式、缓存和外部证书生成均保持在可信核心之外。

## 12. 形式对象与规则层

- `Player` 为二元归纳类型，`Player.other` 实现对手 involution。
- `Cell := empty | stone Player`，排除单格多重占有。
- `Coord := Fin 15 × Fin 15`，边界由类型保证。
- `Board.cell : Coord → Cell` 以全函数表示棋盘。
- `Position := Board × Player` 记录棋盘和行棋方。
- `Board.place` 是无前置检查的数据更新；`legalMove` 单独施加非终局与空点条件。
- `Reachable` 是从 `initial` 经合法 `play` 闭包生成的归纳谓词。

已证明合法落子保持其他格、目标格写入当前方、己方计数增加、对方计数不变、空点数严格下降，以及可达局面的轮次/计数不变量。`reachable_not_both_winners` 无额外非终局前提，覆盖刚形成胜局的可达终局。

## 13. 几何层

`Direction` 仅含横、竖和两条无向对角线；反向通过负偏移表达。`step : Coord → Direction → Int → Option Coord` 统一处理有界步进，越界为 `none`。

核心谓词为：

- `occupiedAt`：某起点沿方向偏移处存在指定颜色棋子；
- `consecutive`：长度为 `n` 的所有有限偏移均被占据；
- `hasRun` 与 `hasAtLeastFive`：存在某起点和方向的连续段；
- `openEnd`：偏移存在且为空，故越界不是开放端；
- `MaximalRun`：连续段左右均不能再由同色棋子延伸。

冻结模式包括 `straightOpenThree`、`straightOpenFour`、两个 `brokenOpenThree` 析取模式和三个 `jumpFour` 析取模式。规范化见证使用 `(Coord × Direction)`，并通过规范起点及可比较重叠条件下的唯一性定理防止同一连续段重复计数，同时不错误排除同方向上互相分离的多条线段。

## 14. 博弈语义

`ForceWin target s` 是归纳博弈语义：

- `terminal`：根已是目标方胜局；
- `choose`：目标方节点存在一个合法子节点继续强制胜；
- `respond`：对手节点的每个合法子节点都继续强制胜。

`CanForceWin s target` 是其公开别名。它自然排除了目标方失败和和棋叶，并在对手节点使用全称量化保证完备应对。

项目还定义位置策略 `Strategy` 与实现关系 `StrategyRealizes`，证明：

```lean
(∃ σ, StrategyRealizes σ s hs) ↔ CanForceWin s target
```

规范获胜策略通过经典选择从 `CanForceWin` 提取，正确性以空位数严格下降进行强归纳。该策略是命题层存在性见证，不是可执行求解器。

## 15. 战术层

战术层刻意分离三种语义：

1. 纯几何图形；
2. 当前轮次、空点和非终局意义下的合法着法；
3. 覆盖对手全部应手的博弈强制性。

`WinningMoves` 依赖当前轮次和完整终局语义；`WinningCells` 则是与当前轮次无关的几何成五点集合。`FourExtensionCells` 单独表示“形成四连的扩展点”，避免把活三扩展误称为立即成五。

已证明：

- 直线活三至少产生四连扩展点；
- 直线活四至少存在成五点；
- 冻结断三模式填内缺口可形成方向性开放四；
- 三种跳四模式填缺口可立即形成黑五连；
- 两个不同的 `WinningCells` 在对手单步防守后仍保留另一个胜点；
- `HasDoubleThreat` 在轮次、非终局和反击排除条件下推出 `CanForceWin`；
- 直线单活四在黑棋回合且非终局时推出 `CanForceWin`。

`SafeDoubleOpenThree` 和 `SafeBrokenOpenThree` 将“所有合法防守后仍有 `CanForceWin`”直接作为语义条件；`ImmediateSafe...` 将其加强为所有防守后均有立即胜着。项目没有证明纯 `GeometricDoubleOpenThree` 自动蕴含安全强制胜，且已有反例测试阻止这种误用。

## 16. 证书层

可信表示 `CertificateTree target s` 以依赖类型把每个子树的根固定为 `play s m`。紧凑表示 `CompactCertificate` 使用节点数组和自然数引用，支持外部生成及 DAG 共享。节点分为：

- `terminal position outcome`；
- `proverMove position move childRef`；
- `opponentMoves position children`。

布尔检查器验证目标、全局根、终局标签、轮次、合法性、引用范围、严格递增引用、子局面一致性以及对手合法着法全覆盖。`compact_reify_at` 对 `nodes.size - index` 强归纳，将通过检查的节点 DAG 重建为 `CertificateTree`。随后由 `CertificateTree.sound` 得到统一的 `CanForceWin`。

局部证书使用相同节点和边检查，但不强制根为初始局面；`checkLocalCertificateAt` 额外核验调用者指定根。这支持战术证书与小型搜索回归，同时不弱化最终全局检查器。

## 17. 搜索与性能基础

现有 `Gomoku.Search` 属于不可信候选生成层，包含：

- 行主序 225 坐标枚举及双向索引定理；
- 参考和优化后的合法候选枚举；
- 邻近优先但集合保持的着法排序；
- 立即胜着优先；
- 对手立即胜点掩码和防守优先实验接口；
- 立即胜证书、两层全应手证书生成；
- 有限深度 `CandidateTree` 搜索及紧凑证书编译；
- 局部检查通过后的 soundness 桥。

`createsFiveFast` 只扫描包含新落点的至多 20 个五格窗口。`createsFiveFast_sound`、`complete` 与 `terminal_iff` 在相应前提下把快速布尔结果连接回规则层。

`PositionKey := Player × Vector Cell 225` 是无损精确键，`positionKey_eq_iff` 排除碰撞合并。`SearchMemo` 已有包括深度、目标方和局面的键及外层命中接口，但尚未真正作为递归状态在整棵搜索中读写，故目前没有形成成熟置换表。

## 18. 验证与剩余风险

当前工程已经通过 `lake build`，核心源码中没有 `sorry` 或 `admit`。测试模块使用 `native_decide` 做可执行回归；核心规则、博弈和证书 soundness 链不依赖它。

尚未完成：

- 实际 15×15 全局策略证书；
- `initial_black_wins`；
- 可扩展的外部高性能搜索器；
- 真正递归读写的置换表；
- 增量终局、空点和威胁维护；
- 对称性约简及保持定理；
- 所有可能五子棋俗称棋形的穷尽模式库；
- 从纯几何双活三到安全强制胜的一般充分性定理。

---

# 第三部分：完全详细、系统的实现说明

## 19. 总体分层：为什么要拆成这些文件

项目依赖方向如下：

```text
Basic
  ↓
Geometry
  ↓
Rules
  ↓
Game
  ↓
Tactics
  ↓
Certificate
  ↓
Search

Examples / Adversarial 负责回归和证伪测试
```

这种顺序体现了数学依赖：如果坐标、落子或五连定义错误，上层所有战术和证书结论都会失去意义；因此底层只放最基础对象，上层逐步增加语义。

各文件职责：

| 文件 | 核心问题 |
| --- | --- |
| `Gomoku/Basic.lean` | 棋盘上有什么对象，怎样更新一个格子 |
| `Gomoku/Geometry.lean` | 什么叫沿方向走、连续、开放端和特定棋形 |
| `Gomoku/Rules.lean` | 什么叫一盘合法进行中的棋，何时结束 |
| `Gomoku/Game.lean` | 什么叫面对所有对手应对仍能获胜 |
| `Gomoku/Tactics.lean` | 局部棋形怎样连接到立即胜和强制胜 |
| `Gomoku/Certificate.lean` | 怎样把一整套应对树编码、检查并变成证明 |
| `Gomoku/Search.lean` | 怎样自动寻找候选应对树并做性能准备 |
| `Gomoku/Examples.lean` | 基础 API 正例 |
| `Gomoku/Adversarial.lean` | 反例、错误证书和端到端局部测试 |

后续章节按这个顺序解释实现。

## 20. `Basic.lean`：把棋盘变成数学对象

### 20.1 玩家和对手

```lean
inductive Player where
  | black
  | white
```

`inductive` 在这里可以理解为“列出这个对象的全部可能情况”。`Player` 只有黑、白两个值，不存在第三种玩家。

```lean
def Player.other : Player → Player
  | .black => .white
  | .white => .black
```

这个函数把玩家映射到对手。项目证明了 `other (other p) = p`，以及玩家不等于自己的对手。后续轮次切换和对手节点都依赖这些简单结论。

### 20.2 格子为什么不是两个真假值

```lean
inductive Cell where
  | empty
  | stone (player : Player)
```

一个格子或者为空，或者装有某一玩家的棋子。没有采用“这里是否有黑棋”和“这里是否有白棋”两个布尔量，因为后者可能表示出同一个位置黑白同时为真的非法状态。当前表示从类型结构上就排除了这种情况。

### 20.3 坐标如何自动保证不越界

```lean
abbrev Coord := Fin 15 × Fin 15
```

`Fin 15` 是带有“小于 15”证明的自然数，可以取 0 到 14。一个坐标是两个这样的数。因此 `(14, 14)` 合法，`(15, 0)` 不能成为 `Coord`。边界约束不是每次调用时才检查，而是坐标类型本身的一部分。

### 20.4 棋盘为什么表示成函数

```lean
structure Board where
  cell : Coord → Cell
```

给棋盘一个合法坐标，它返回该格内容。这在数学上和一个由 225 个位置组成的有限映射等价。函数表示使“查询任意位置”直接可用，也方便用函数外延性证明两个棋盘相等。

空棋盘把每个坐标映射到 `empty`：

```lean
def Board.empty : Board := ⟨fun _ => .empty⟩
```

原始更新 `Board.place b c p` 在 `c` 处返回 `stone p`，在其他位置继续返回原棋盘内容。它只是数据更新，不检查空点或终局。这一设计很重要：

- `Board.place` 回答“如果把这个值写进去，数据是什么”；
- `legalMove` 回答“按照五子棋规则，这次写入能不能发生”。

把二者分开使底层函数简单，但也要求任何战术结论不能只使用 `place` 或 `play`，还必须携带合法性前提。对抗性审计正是针对这种分层做过检查。

### 20.5 底层不变量怎样证明

关键定理包括：

```lean
Board.place_same
Board.place_other
Board.count_place_same_of_empty
Board.count_place_other_of_empty
Board.emptyCount_place_of_empty
```

前两个定理分别说更新点发生变化、其他点保持不变。后三个定理对 225 个格子构成的有限集合进行筛选和计数，证明在空点放入玩家棋子后：

- 该玩家棋子数加一；
- 对手棋子数不变；
- 空点数减一。

这些不是测试几个具体棋盘，而是对任意棋盘、任意坐标和任意玩家成立的全称定理。

## 21. `Geometry.lean`：把“同一条线”说清楚

### 21.1 四个方向为什么已经足够

```lean
inductive Direction where
  | horizontal
  | vertical
  | diagonalUp
  | diagonalDown
```

没有另外列出“向左、向下”等反方向，因为方向允许整数偏移。沿横向走 `1` 和走 `-1` 就分别表示两个相反方向。这样只用四个无向轴就覆盖所有五连方向。

每个方向有 `(dx, dy)`：横向为 `(1, 0)`，纵向为 `(0, 1)`，两条对角线为 `(1, 1)` 和 `(1, -1)`。

### 21.2 `step` 如何统一处理边界

```lean
def step (c : Coord) (d : Direction) (n : Int) : Option Coord
```

它从 `c` 沿 `d` 走整数步 `n`。如果结果还在 0 到 14 内，返回 `some q`；越界则返回 `none`。

`Option Coord` 可以理解为“可能有一个坐标，也可能不存在”。因此所有边界情况都必须显式处理，不能凭空把棋盘外当成一个空格。项目还证明 `step_reverse`：如果从 `c` 走 `n` 步到 `q`，从 `q` 走 `-n` 步会回到 `c`。

### 21.3 连续棋子的量词结构

```lean
def occupiedAt ... (n : Int) : Prop :=
  ∃ q, step c d n = some q ∧ b.cell q = .stone p
```

这表示偏移 `n` 的位置必须存在，并且那里是玩家 `p` 的棋子。

```lean
def consecutive ... (length : Nat) : Prop :=
  ∀ n : Fin length, occupiedAt b p c d n.1
```

这表示从偏移 0 到 `length - 1` 的每个位置都有该玩家棋子。

```lean
def hasRun ... (length : Nat) : Prop :=
  ∃ c d, consecutive b p c d length
```

这再对起点和方向取存在量词。`hasAtLeastFive` 被定义为存在长度 5 的连续段。一个六连包含长度 5 的子段，因此无需另写“六连”规则也自然满足胜利条件。

### 21.4 开放端和最大连续段

```lean
def openEnd ... (n : Int) : Prop :=
  ∃ q, step c d n = some q ∧ b.cell q = .empty
```

开放端要求那个位置真实存在且为空。越界时 `step = none`，所以开放端命题为假。这就是“边界算封闭”的精确实现。

```lean
def MaximalRun ... (length : Nat) : Prop :=
  consecutive ... length ∧
  ¬ occupiedAt ... (-1) ∧
  ¬ occupiedAt ... length
```

最大连续段不是“全棋盘最长的那条线”，而是“这一段向左右都不能再接上同色棋子”。例如一条恰好四颗的连续黑棋，如果相邻两侧都不是黑棋，它就是长度 4 的最大连续段。这里的“最大”是包含意义下不能延伸，不是长度和全盘其他线比较后的最大值。

### 21.5 棋形表如何冻结

直线活三是三个连续同色棋子，偏移 `-1` 和 `3` 都为空；直线活四同理使用四颗棋子和偏移 `-1`、`4`。

断三和跳四采用有限析取表。例如断三只冻结：

```text
空 棋 棋 空 棋 空
空 棋 空 棋 棋 空
```

跳四冻结五格内四子一缺口的三种内部缺口位置。每个模式还明确要求外侧和缺口为空。增加其他五子棋流派中的形状时，必须增加新的析取或新谓词，不能仅靠自然语言把它们算进来。

### 21.6 为什么需要规范化和唯一性

战术层会把 `(起点, 方向)` 收集成有限集合。如果同一连续段能从多个起点被重复记录，“有两个活三”的计数就可能是假的。

`canonicalRunStart` 要求起点前一格不是同色棋子；开放三、开放四由于前一格必须为空，自然满足这个条件。`StartShiftConflict` 和 `ComparableRunStarts` 描述两个起点是否位于同一段的可比较偏移位置。项目证明：两个规范化开放三或开放四如果起点可比较，那么起点必相同。

该定理没有声称同一方向全盘只能有一条活三。两条空间分离的活三仍然是两个合法见证。

## 22. `Rules.lean`：从静态棋盘变成一盘合法对局

### 22.1 `Board` 与 `Position` 的区别

`Board` 只记录格子内容；同一张棋盘若轮到不同玩家，博弈意义不同。因此：

```lean
structure Position where
  board : Board
  turn : Player
```

`Position` 即“局面”，由棋盘和当前行棋方组成。`initial` 是空棋盘且轮到黑棋。

### 22.2 终局有两种接口

```lean
def isTerminal (s : Position) : Prop :=
  黑有五连 ∨ 白有五连 ∨ 棋盘已满
```

这是命题接口，用在证明前提中。

```lean
def terminal (s : Position) : Option Outcome := ...
```

这是可计算接口，返回 `blackWin`、`whiteWin`、`draw` 或 `none`。检查顺序是黑胜、白胜、满盘和棋。对任意人工乱写的棋盘，如果双方都五连，它会先返回黑胜；但项目证明这种双方同时胜利的状态不会从合法对局中到达，所以优先级不影响正常博弈。

### 22.3 合法着法到底包含什么

```lean
def legalMove (s : Position) (c : Coord) : Prop :=
  ¬ isTerminal s ∧ s.board.cell c = .empty
```

合法着法只要求：

1. 对局尚未结束；
2. 目标格为空。

落下哪种颜色不需要作为额外参数，因为 `play` 总是放入 `s.turn` 的棋子，然后切换到 `s.turn.other`。无禁手正体现为这里没有三三、四四或长连排除条件。

### 22.4 “可达局面”的归纳定义

```lean
inductive Reachable : Position → Prop where
  | initial : Reachable initial
  | step : Reachable s → legalMove s c → Reachable (play s c)
```

这与数学中的“由初始对象在某关系下生成的最小闭包”相同。可达局面只有两种来源：初始局面，或者从已经可达的局面走一手合法棋得到。

`Reachable` 没有存储具体历史棋谱，只证明存在某条合法历史到达当前局面。这足以推出轮次、计数和双方不同时获胜等全局不变量。

### 22.5 规则层重要证明链

项目证明：

- `terminal_no_legal`：终局与合法着法矛盾；
- `exists_legalMove_of_terminal_none`：非终局一定还有合法着法；
- `play_preserves_other_cells`：合法落子外的格子不变；
- `play_target_cell`：目标格成为当前玩家棋子；
- `play_emptyCount_lt`：每步空格数严格下降；
- `reachable_count_invariant`：轮次与黑白数量对应；
- `reachable_not_both_winners`：可达局面不能双方同时五连。

空格严格下降既表达棋局有限，也给后面从获胜命题提取具体策略时提供良基归纳尺度。

## 23. `Game.lean`：严格区分“有一条赢法”和“无论怎样都能赢”

### 23.1 普通存在量词为什么不够

如果只证明“存在一串着法最后黑棋赢”，这可能依赖白棋故意配合。强制获胜包含交替的存在量词和全称量词：

```text
黑棋节点：存在一个黑棋选择……
白棋节点：对所有白棋合法选择……
黑棋节点：再存在一个黑棋选择……
……最终黑棋胜利。
```

这正是有限、确定、完全信息、双方轮流行动的二人博弈语义。

### 23.2 `ForceWin` 的三个构造规则

```lean
inductive ForceWin (target : Player) : Position → Prop where
  | terminal ...
  | choose ...
  | respond ...
```

目标玩家固定为 `target`。三个构造器分别表示：

1. `terminal`：当前局面已经由 `terminal s = some (winner target)` 确认为目标方胜利。
2. `choose`：局面未结束且轮到目标方；给出一手合法棋，并给出子局面仍满足 `ForceWin` 的证明。
3. `respond`：局面未结束且轮到对手；对任意坐标 `m` 和它合法的证明，都必须给出对应子局面的 `ForceWin`。

公开名字：

```lean
def CanForceWin (s : Position) (target : Player) : Prop :=
  ForceWin target s
```

它不是一个会运行并输出真假的普通搜索函数，而是一个数学命题。构造该命题的证明，就等于构造符合上述存在/全称结构的有限获胜树。

### 23.3 什么是 `Strategy`

```lean
def Strategy (target : Player) :=
  ∀ s, Reachable s → s.turn = target → terminal s = none →
    {m : Coord // legalMove s m}
```

策略是一个函数：每当给它一个可达、未结束且轮到目标玩家的局面，它都返回一个坐标，同时附带该坐标合法的证明。花括号类型 `{m // legalMove s m}` 是“一个值加上关于这个值的性质证明”。

但这个类型只保证策略不犯规，不保证它会赢。所以项目另定义 `StrategyRealizes σ s hs`：

- 目标方节点必须严格使用 `σ` 返回的那一步；
- 对手节点仍要包含全部合法应对；
- 叶子必须是目标方胜利。

这避免把“会合法落子的软件”误称为“获胜策略”。

### 23.4 策略、获胜树和命题为何等价

`StrategyRealizes.sound` 直接递归翻译三个构造器，得到 `CanForceWin`。反方向中，`canonicalWinningStrategy` 在每个已经知道可强制获胜的目标方节点，使用经典选择挑出一个保持胜势的合法着法；在不属于获胜区域的局面任取合法着法。

证明这个规范策略确实获胜时，以 `Board.emptyCount` 做强归纳：每走一步空点严格减少，所以递归调用一定处理更小的自然数。最终得到：

```lean
(∃ σ, StrategyRealizes σ s hs) ↔ CanForceWin s target
```

这里的经典选择不提供高效可执行算法；它证明的是数学上的存在等价。真正计算策略仍需要搜索器和证书。

## 24. `Tactics.lean`：从局部形状到博弈结论

### 24.1 见证集合

`openThreeWitnesses` 和 `openFourWitnesses` 遍历全部 225 个起点与四个方向，把满足规范化棋形的 `(起点, 方向)` 收进有限集合。

`card = 1` 表示恰好一个见证，`2 ≤ card` 可转换为存在两个互不相同的见证。这里统计的是严格定义的模式见证，不是人类凭视觉认为的“两个威胁”。

### 24.2 `WinningMoves` 与 `WinningCells` 的区别

`WinningMoves s p` 收集在当前真实博弈中玩家 `p` 可以合法落下且使 `terminal (play s m)` 成为 `winner p` 的坐标。因此它包含轮次和非终局要求。

`WinningCells s p` 只收集当前为空、假设放入 `p` 后会形成五连的格子，不要求当前轮到 `p`。它适合分析“对手下一步威胁在哪里”。

`HasImmediateWin` 是 `WinningMoves` 非空；`HasDoubleThreat` 是 `WinningCells` 至少有两个不同元素。两者不能互换。

### 24.3 为什么另设四连扩展点

一个活三下一步通常只是变成四，不是直接五连。因此：

- `FourExtensionCells`：落子后形成某个四连；
- `WinningCells`：落子后形成五连。

`straightOpenThree_has_fourExtension` 只证明前者。十字双活三反例验证：即使存在多个四连扩展点，当前立即成五点集合仍可能为空。这个中间层阻止了把“造四”偷偷提升成“立即获胜”。

### 24.4 双立即胜点定理的证明思路

假设目标玩家有两个不同的空格 `a ≠ b`，任意一个落下都能成五。轮到对手时，对手一次只下一颗棋：

- 若对手下在 `a`，则 `b` 没被占；
- 若对手下在其他位置，则至少可以选择一个仍为空的胜点；
- 对手自己的落子不能消除已经存在于其他格上的目标方棋子；
- 还要显式排除对手已经能够立即获胜。

`winningCell_ne_of_hasDoubleThreat` 提取与防守点不同的另一个胜点；后续引理证明它在防守后仍能形成目标方五连；`doubleThreat_forces_win` 最终用 `ForceWin.respond` 覆盖每个对手合法应手，再在每个分支用立即胜着收尾。

### 24.5 活四和跳四怎样证明立即胜

`straightOpenFour_black_immediate` 从开放四的右端空点取得坐标 `m`：

1. 非终局和空点给出 `legalMove s m`；
2. 原四颗棋子通过 `Board.place_other` 保持不变；
3. 新位置通过 `Board.place_same` 成为黑棋；
4. 五个偏移逐个组成 `consecutive ... 5`；
5. 终局函数因此返回 `blackWin`。

`fillGapFive_black_immediate` 把“五格内只有一个缺口”的公共逻辑抽出；`jumpFour_black_immediate` 对三个冻结缺口位置分别调用它。

### 24.6 “几何双活三”与“安全双活三”

```lean
def GeometricDoubleOpenThree ... :=
  落子后的开放三见证数至少为二

def DoubleOpenThree ... :=
  当前轮次正确 ∧ 着法合法 ∧ GeometricDoubleOpenThree ...
```

即使 `DoubleOpenThree` 成立，也只说明落子合法且产生两个几何见证。真正的安全接口还要求：

```lean
def SafeDoubleOpenThree ... :=
  DoubleOpenThree ... ∧
  落子后仍未终局 ∧
  对手没有立即胜着 ∧
  对每个对手合法防守，防守后目标方仍 CanForceWin
```

于是 `safeDoubleOpenThree_forces_win` 的证明很短：第一步用 `ForceWin.choose`，随后用 `ForceWin.respond`，而所有防守后的获胜证明已经包含在安全条件中。

这条定理是一个正确的组合接口，但并没有解决最难的部分：怎样仅从一个容易检查的几何条件推出“每个防守后仍可强制胜”。后者仍需要更多战术引理或策略证书。断三的安全接口采用同样设计。

## 25. `Certificate.lean`：可检查的完整应对说明书

### 25.1 为什么先有依赖类型树

```lean
inductive CertificateTree (target : Player) : Position → Type
```

这个树的类型直接带着根局面。`proverMove` 的子树类型必须是 `CertificateTree target (play s m)`；因此手工构造这种树时，不能把无关棋盘冒充下一局面。`opponentMoves` 的 children 是一个函数，输入任何合法着法及其证明，都返回对应子树，所以天然覆盖全部应手。

`CertificateTree.sound` 逐构造器转换为 `ForceWin`。反方向也证明了 `CanForceWin` 可产生一个非空证书树。因此可信树和博弈语义表达同一件事。

### 25.2 为什么还需要紧凑证书

依赖类型树适合证明，但外部程序很难直接生成大量依赖证明。紧凑证书改用普通数组：

```lean
inductive CertificateNode where
  | terminal (position : Position) (winner : Outcome)
  | proverMove (position : Position) (move : Coord) (child : Nat)
  | opponentMoves (position : Position)
      (children : Array (Coord × Nat))

structure CompactCertificate where
  target : Player
  root : Nat
  nodes : Array CertificateNode
```

子节点用自然数索引引用。多个父节点还可以共享同一后续节点，形成无环有向图，而不必复制完全相同的子树。代价是数组可能包含大量错误，所以必须完整检查。

### 25.3 节点检查的精确内容

终局节点检查：

- 写入的局面真实终局；
- 写入的结果等于目标玩家胜利。

目标方节点检查：

- 当前未终局；
- 当前轮到目标方；
- 指定着法合法；
- 子引用在数组范围内；
- 子引用严格大于父索引；
- 子节点记录的局面等于 `play s m`。

对手节点检查：

- 当前未终局；
- 当前轮到目标方的对手；
- 每个列出的着法合法；
- 每个引用有效且严格向后；
- 每个子局面匹配对应落子；
- 当前局面的每一个合法着法都出现在 children 中。

最后一项是最关键的完备性检查。只记录一条白棋主线，即使那条主线黑棋获胜，也会因为遗漏其他白棋应对而被拒绝。

### 25.4 为什么严格向后引用能够排除循环

对每条边要求 `parent < child`。沿任意路径，节点索引必须严格递增，而所有索引又小于有限数组长度。因此不可能绕回已经访问的较小索引，也不可能形成无限路径。

这还为重建证明提供下降量：从索引 `i` 开始，剩余可用节点数是 `nodes.size - i`；进入更大的子索引时该自然数严格变小。

### 25.5 布尔检查怎样变成数学证明

可执行检查器返回 `Bool`，例如 `checkNodeAt ... = true`。项目为每种检查证明对应的 `..._iff` 定理，把布尔等式拆为普通命题：合法性、范围、局面相等等。

`compact_reify_at` 使用这些命题，对 `nodes.size - i` 强归纳：

- 终局节点直接构造 `CertificateTree.terminal`；
- 目标方节点先递归重建子树，再构造 `proverMove`；
- 对手节点利用全覆盖性质，为任意合法着法找到数组中的对应引用，递归重建其子树，再构造 `opponentMoves`。

因此链条是：

```text
布尔检查为 true
  → 每个节点的命题事实
  → 重建依赖类型 CertificateTree
  → CertificateTree.sound
  → CanForceWin
```

全局 `checkCertificate` 还要求目标为黑、根索引有效、根局面等于 `initialPosition`。于是 `compact_certificate_sound` 的结论正是空棋盘黑棋强制胜。

### 25.6 局部证书为何不是放宽全局证明

战术测试常从一个人工局面开始，所以 `checkLocalCertificate` 不要求空棋盘根；`checkLocalCertificateAt s c` 会检查证书根是否等于指定局面 `s`。其他节点、边、覆盖、终局检查全部复用全局检查器。

局部证书只能推出 `CanForceWin s target`，不能凭空推出初始局面获胜。最终定理仍必须走具有初始根约束的全局接口。

## 26. `Search.lean`：寻找候选，而不是替代证明

### 26.1 搜索层为什么不在可信核心内

搜索器可以有错误、启发式可以漏掉有用着法、缓存可以写错、外部程序甚至可能崩溃。只要它输出的候选最终由证书检查器重新核验，这些错误最多造成“找不到证明”或“证书被拒绝”，不会把假结论变成 Lean 定理。

因此搜索层的所有优化原则都是：可以改变寻找顺序和速度，但成功结果必须经过同一个检查器。

### 26.2 坐标枚举与索引

`allCoords` 使用 `Fin 225` 构造 225 个行主序坐标。`coordIndex (x, y) = 15*x + y`，`coordAtIndex` 用除法和余数反解。项目证明两个方向的复合都是恒等，这使长度 225 的数组或布尔向量可以安全地按坐标直接访问。

### 26.3 候选着法和排序

`candidateMoves` 是参考实现：遍历全部坐标并逐点判断 `legalMove`。`candidateMovesFast` 把“局面是否终局”提到过滤循环之外，只在非终局时筛空格。成员等价定理证明优化版本没有增加或删除候选。

`orderedCandidateMoves` 把附近已有棋子的空格排在前面，但两个过滤数组再拼接，所有合法候选仍保留。`immediateWinningMovesFirst` 再把 `createsFiveFast = true` 的着法放在前面。排序只影响找到解的时间，不影响对手节点最终必须覆盖全部合法着法。

### 26.4 快速成五检查为何正确

新落一颗棋后，新产生的五连一定包含这颗棋。它在某个方向的五格窗口中可能处于第 0、1、2、3 或 4 个位置；四个方向乘五个位置，至多 20 个窗口。

`createsFiveFast_sound` 从某个通过的窗口直接构造 `hasAtLeastFive`。`createsFiveFast_complete` 假设原棋盘没有目标方五连，证明落子后的任何新五连都必须包含新落点，从而落入这 20 个窗口之一。`createsFiveFast_terminal_iff` 再结合合法轮次与终局定义，连接到真实胜负标签。

### 26.5 当前搜索器能做什么

`firstWinningMove` 找一步立即胜着。`twoPlyCertificateFor` 在轮到对手的局面枚举全部应手，并要求每个应手后目标方都有一步立即胜着。

更一般的 `searchCandidateTree fuel s target` 做有限深度递归：

- 已是目标方胜局，返回终局候选；
- 已是其他终局，失败；
- 深度为零且未终局，返回 `none`，表示未在该深度证明；
- 目标方节点依次尝试着法，找到一个成功子树即可；
- 对手节点要求每个合法着法都搜索成功。

`none` 不能解释为“目标方不能赢”，它也可能只表示深度太浅或搜索太慢。成功的 `CandidateTree` 会编译成 `CompactCertificate`，并在检查成功后才由 `checkedDepthCertificateFor_sound` 推出 `CanForceWin`。

### 26.6 局面键与缓存当前到哪一步

`PositionKey` 保存当前玩家和按固定顺序排列的全部 225 个格子。`positionKey_eq_iff` 证明键相等当且仅当局面相等，所以它不是可能碰撞的短哈希。

`SearchKey` 还加入剩余深度和目标玩家，因为同一棋盘在不同搜索深度或不同目标下不是同一个查询。`SearchMemo` 已能查找和插入候选结果，并有命中/未命中接口；但现有递归函数没有把更新后的表在子节点和兄弟节点之间传递。因此它目前是安全的适配基础，不是已经带来规模提升的完整置换表。

## 27. 测试和对抗性审计怎样组织

### 27.1 定理、`example` 和计算测试的区别

命名 `theorem` 通常是供其他证明复用的通用结论。`example` 也必须通过 Lean 检查，但没有可引用的全局名字，适合回归具体局面。

测试模块的 `native_decide` 让 Lean 编译并执行一个可判定命题，适合含 225 格扫描的具体例子。它依赖 Lean 编译器运行时，信任范围大于纯内核证明，所以项目把它用于测试和样例，而不让核心证书 soundness 定理依赖它。

### 27.2 已覆盖的正向例子

- 初始轮次和空点；
- 横、竖、两条对角线成五；
- 边界窗口；
- 六连计胜；
- 直线开放三和开放四；
- 冻结断三与跳四；
- 两个独立即胜点；
- 两层对手全分支证书；
- 候选树编译并通过局部检查；
- 深度 2 小局面搜索闭环；
- 快速成五与参考终局检查一致。

### 27.3 已覆盖的负向和变异例子

- 占用格不能合法落子；
- 边界不是开放端；
- 断三不会自动识别为直线活三；
- 对手已有立即胜着时，安全双活三谓词拒绝；
- 几何双活三不能自动得到防守后立即胜；
- 证书遗漏一个对手应手；
- 子引用越界；
- 子局面与落子不匹配；
- 终局标签错误；
- 循环或不向后的引用；
- 错误全局根和空证书。

`AUDIT_REPORT.md` 记录这些发现、修正和仍开放的风险。测试通过只能支持它所覆盖的性质，不能代替对所有局面的通用定理，也不能代替尚不存在的全局策略证书。

## 28. 当前可信链的完整图景

```text
Fin 15 坐标 + Cell/Board
        ↓
有界 step + consecutive + hasAtLeastFive
        ↓
Position + legalMove + terminal + Reachable
        ↓
ForceWin / CanForceWin
        ↑
CertificateTree.sound
        ↑
compact_reify_at
        ↑
checkCertificate c = true
        ↑
外部或内部搜索器生成 CompactCertificate 候选
```

底部搜索器可以替换，不改变上面的可信证明。未来的最终证书只需通过现有检查器，就能接入这条链。

## 29. 成果状态总表

| 内容 | 当前状态 | 能推出什么 | 不能冒充什么 |
| --- | --- | --- | --- |
| 15×15 无禁手规则 | 已形式化并证明基础不变量 | 合法对局语义明确 | 黑棋必胜 |
| 直线活三/活四、部分断三/跳四 | 已冻结模式和若干桥接定理 | 识别这些明确形状 | 所有民间棋形大全 |
| 单活四立即胜 | 已证明 | 满足前提的局面黑可强制胜 | 任意有“四”的局面都赢 |
| 双立即胜点 | 已证明带安全前提的强制胜 | 两个独立成五点的局部胜势 | 几何双活三自动必胜 |
| `CanForceWin` | 已完成统一语义 | 严格表达必胜策略树 | 一个自动求解算法 |
| 策略等价性 | 已证明 | 获胜策略、策略树、强制胜语义一致 | 已经算出全局策略 |
| 紧凑证书检查器 | 已实现并证明 soundness | 通过的全局证书可推出黑棋初始必胜 | 已经存在全局证书 |
| 对抗性审计 | 已完成当前批次 | 已知错误变体会被拒绝 | 绝对不存在未知缺陷 |
| 有限深度搜索 | 小局面闭环已完成 | 自动产生并核验浅层局部证书 | 可解决完整 15×15 |
| 精确局面键和缓存接口 | 基础完成 | 为置换表提供无碰撞键 | 已经实现高性能缓存搜索 |
| `initial_black_wins` | 未完成 | 无 | 不得提前声称已证明 |

## 30. 从现在到最终目标还需要什么

下一阶段的主要矛盾是搜索规模，不是规则层缺失。建议路线：

1. 先审计当前搜索代码，清理临时接口并建立性能基线。
2. 实现真正携带并更新状态的递归缓存，区分已胜、已败和深度不足。
3. 增量维护空点、最后落点、终局信息和双方立即胜点，减少整盘重复扫描。
4. 加强必须防守优先、威胁空间搜索等候选排序，但保持对手分支完备。
5. 用一系列逐渐扩大的已知局面验证搜索和证书生成。
6. 将大规模搜索移到独立高性能程序，输出 Lean 可表示的紧凑证书。
7. 评估证书体积、共享节点和分块导入方案。
8. 让 Lean 运行 `checkCertificate` 并使用 `compact_certificate_sound`。
9. 只有上述全局证书真实通过后，声明 `initial_black_wins`。

对称性压缩可在性能需要时加入，但必须证明旋转/反射保持坐标有效性、落子、终局和强制胜；否则只在不可信搜索器内部用于找候选，最终证书仍展开并由普通检查器验证。

## 31. 第一份文档的结论

项目当前最有价值的成果不是一句尚未得到的“黑棋必胜”，而是一套已经能够区分以下内容的严格基础：

- 什么是合法五子棋；
- 什么是特定棋形；
- 什么是一步获胜；
- 什么是覆盖对手所有选择的强制获胜；
- 什么只是搜索候选；
- 什么样的有限说明书可以被 Lean 接受为证明；
- 哪些局部结论已经证明，哪些全局结论仍然缺失。

这套分层使后续团队可以更换搜索方法、扩展棋形或增加优化，而不必重新相信一个复杂外部程序。最终困难仍然很大，但目标已经被拆成一个明确的工程任务：生成真实完整证书，并通过已证明正确的检查链。
