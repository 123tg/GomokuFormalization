# Formalization walkthrough

The maintained walkthrough is now
[`LOCAL_PROJECT_LEARNING_GUIDE.md`](LOCAL_PROJECT_LEARNING_GUIDE.md). It follows
the fixed 7×7 model from `Coord` and board geometry through rules, game
semantics, certificates, search, and the C++ exporter.

This short redirect replaces the obsolete pre-migration walkthrough so that
the repository has only one current description of the model.
