# 无禁手五子棋形式化项目：学习、理解与接手指南

> 本文档面向数学系本科生。假设读者会阅读集合、函数、量词、归纳定义和初等证明，但几乎没有独立编程经验，对 Lean 只接触过基础语法。
>
> 本文档只作为本地学习资料，不需要上传 GitHub。正确用法是打开相应源码，按章节做少量练习，再回来继续。

## 1. 学习目标：你最终需要会什么

你不必立刻学会独立写出几百行 Lean 证明，也不必亲自实现高性能搜索器。作为项目发起者和后续工作的指导者，你首先需要五种能力：

1. **解释能力**：能用自己的话说明每个模块解决什么问题。
2. **读定理能力**：看到一个 Lean 定理，能分清变量、前提和结论。
3. **语义审查能力**：能判断一个定义是否真的对应你想要的五子棋概念。
4. **边界意识**：能区分已经证明、只通过具体测试、只有接口、仍未完成的内容。
5. **任务规划能力**：能把需求分配到规则、几何、战术、证书或搜索层，并规定验收标准。

达到目标以后，你应当能提出这些问题：

- 这个定理要求当前轮到黑棋吗？
- 这里使用纯棋形条件，还是已经包含合法落子？
- 对手节点是否覆盖每个合法应手？
- 搜索返回 `none` 是证明失败，还是只表示深度不足？
- 这个优化是否有定理证明它没有删掉合法候选？
- 这是具体例子的计算结果，还是对任意局面的通用定理？

---

# 第一篇：用数学思维理解 Lean

## 2. Lean 在这个项目里扮演什么角色

Lean 同时是三样东西：

1. 一种描述数学对象和计算过程的语言；
2. 一个检查证明是否合法的系统；
3. 一个能够执行有限计算、构造数据的程序环境。

可以把 Lean 想成一个非常严格的数学助教：

- 它不判断你的定义是否符合你心中的五子棋，只判断定义是否写得合法；
- 一旦定义确定，它会严格检查证明是否真的从前提推出结论；
- 因此人负责语义是否选对，Lean 负责推理是否遵守选定语义。

错误定义也可能有完全正确的 Lean 证明。所以项目既需要形式证明，也需要自然语言审查和反例测试。

## 3. 类型对应“对象所属的集合”

数学中写 `x ∈ A`。Lean 中常写：

```lean
x : A
```

读作“`x` 的类型是 `A`”。例如：

```lean
p : Player
c : Coord
s : Position
```

函数类型 `Coord → Cell` 对应从坐标到格子内容的函数。多参数类型

```lean
Board → Coord → Player → Board
```

初学时可以读成三元函数：输入棋盘、坐标和玩家，输出新棋盘。

## 4. `Type` 与 `Prop`

`Type` 中的对象通常是数据，例如玩家、坐标、棋盘。`Prop` 中的对象是命题，例如“这个位置为空”。

```lean
Player : Type
legalMove s c : Prop
```

若 `h : P`，可理解为 `h` 是命题 `P` 已成立的凭据。

## 5. 四种基本声明

### 5.1 `def`：下定义

```lean
def winner : Player → Outcome
  | .black => .blackWin
  | .white => .whiteWin
```

### 5.2 `structure`：把字段组成对象

```lean
structure Position where
  board : Board
  turn : Player
```

若 `s : Position`，则 `s.board` 是棋盘，`s.turn` 是当前玩家。

### 5.3 `inductive`：列出全部构造方式

```lean
inductive Outcome where
  | blackWin
  | whiteWin
  | draw
```

归纳命题也用 `inductive`。`Reachable` 列出局面可达的两种证明方式，`ForceWin` 列出强制获胜的三种证明方式。这与归纳定义自然数、生成闭包或树相近。

### 5.4 `theorem`：声明并证明结论

```lean
theorem play_emptyCount_lt {s : Position} {c : Coord}
    (hlegal : legalMove s c) :
    Board.emptyCount (play s c).board < Board.emptyCount s.board := by
  ...
```

读作：对任意 `s`、`c`，若 `c` 是合法着法，则走完后空格数严格减少。

## 6. Lean 逻辑符号翻译表

| Lean | 数学/中文 | 项目例子 |
| --- | --- | --- |
| `∀ x, P x` | 对所有 `x` | 对手每种合法应手都有后续胜法 |
| `∃ x, P x` | 存在 `x` | 存在一个立即胜着 |
| `P ∧ Q` | `P` 且 `Q` | 非终局且目标格为空 |
| `P ∨ Q` | `P` 或 `Q` | 黑胜或白胜或满盘 |
| `¬ P` | 非 `P` | 不是终局 |
| `P → Q` | 若 `P` 则 `Q` | 合法落子推出空格减少 |
| `P ↔ Q` | 当且仅当 | 快速检查与完整语义等价 |
| `x = y` | 相等 | 子局面等于落子后的局面 |
| `x ≠ y` | 不相等 | 两个胜点不同 |

括号 `(h : P)` 通常是一个命名为 `h` 的前提。花括号 `{x : A}` 通常是可由 Lean 推断的隐式参数，阅读时仍理解成“对任意 `x : A`”。

## 7. 常见数据容器

### 7.1 `Option A`：可能有，也可能没有

值为 `some a` 或 `none`。`step` 越界、`terminal` 非终局、搜索未找到结果都会返回各自语境下的 `none`。搜索的 `none` 不表示已经证明不能赢。

### 7.2 `Fin n`：小于 `n` 的自然数

`i : Fin 15` 带有自然数值和它小于 15 的证明，用于坐标和固定长度偏移。

### 7.3 `List`、`Array`、`Finset`

- `List`：有顺序、递归方便；
- `Array`：有顺序、适合按索引执行和存储证书；
- `Finset`：数学有限集合，不含重复，适合计数不同见证。

### 7.4 子类型 `{x : A // P x}`

它包含一个值以及该值满足性质的证明。策略返回 `{m : Coord // legalMove s m}`，所以既给坐标也给合法性。

## 8. `Bool` 和 `Prop` 为什么同时存在

`Prop` 适合数学陈述，`Bool` 适合实际运行：

```lean
legalMove s c : Prop
checkCertificate c : Bool
```

项目用许多 `..._iff` 定理把 `check... = true` 转换为合法性、引用范围、局面相等等普通命题。这种“可执行判定 + 正确性定理”是形式化验证的核心结构。

## 9. 初学时需要认识的证明命令

| 命令 | 直觉 |
| --- | --- |
| `rfl` | 两边按定义展开后相同 |
| `exact h` | 用已有证明 `h` 完成目标 |
| `intro h` | 引入变量或前提 |
| `apply T` | 使用定理 `T`，留下它需要的前提 |
| `rw [h]` | 用等式 `h` 改写 |
| `simp [...]` | 按定义和规则自动化简 |
| `constructor` | 拆开合取或双向命题 |
| `rcases h with ⟨a, ha⟩` | 拆开存在量词或合取 |
| `cases x` | 对各种构造情况分类 |
| `induction h` | 按归纳定义归纳 |
| `omega` | 解决自然数/整数线性算术 |
| `native_decide` | 编译执行一个具体可判定命题 |

阅读证明时先明确目标、使用的核心前提和最后调用的定理，不必立即逐行模拟自动化。

---

# 第二篇：项目核心术语词典

这一篇不是要求你背定义，而是帮助你把日常五子棋语言翻译成 Lean 可以检查的语言。每个词都按四个层次解释：直觉、严格含义、源码对象、审查时要问的问题。

## 10. `Board`：只记录格子，不记录现在该谁走

直觉上，`Board` 就是一张棋盘照片。它只回答“某一个坐标现在是空的、黑子还是白子”。

```lean
structure Board where
  cell : Coord → Cell
```

这里把棋盘表示成一个函数：给它坐标，它返回该格内容。固定棋盘为 `15 × 15`，因为 `Coord` 是 `Fin 15 × Fin 15`。坐标从 0 到 14；例如日常说的第 8 列、第 8 行，在源码中常写 `(7, 7)`。

`Board` 不知道：

- 现在轮到谁；
- 这张棋盘是否按规则一步步走出来；
- 比赛是否已经结束；
- 下一步是否允许继续下。

`Board.place b c p` 是底层“把 `p` 的棋子写到 `c`”操作。它本身不会检查 `c` 是否为空，因此不能把任何一次 `Board.place` 都叫作合法下棋。正式对局必须通过 `legalMove` 加 `play` 使用它。

审查问题：一段代码是在研究任意静态棋形，还是在声称这是实际对局中的局面？若是后者，仅有 `Board` 通常不够。

## 11. `Position`：棋盘加上当前行棋方

```lean
structure Position where
  board : Board
  turn : Player
```

`Position` 是一张棋盘照片再加一张“现在轮到谁”的标签。项目中的正式行棋、终局和策略都以它为输入。

不过，任意拼出来的 `Position` 仍未必真的能从空棋盘走到。例如可以手工构造一张有十个白子、没有黑子、却写着轮到黑棋的 `Position`。Lean 允许研究它，但它没有自动成为合法对局历史的一部分。

这正是为什么还需要 `Reachable`。

## 12. 历史与 `Reachable`：局面能否从开局按规则走到

“可达局面”意为：从空棋盘、黑棋先手开始，反复进行合法落子，最终能够得到这个局面。

```lean
inductive Reachable : Position → Prop where
  | initial : Reachable Position.initial
  | step : Reachable s → legalMove s c → Reachable (play s c)
```

这是数学中的归纳闭包：

1. 初始局面可达；
2. 从一个可达局面走一步合法棋，得到的局面仍可达；
3. 除了由这两条规则产生的情况，不另行承认可达。

`Reachable s` 并不是把完整历史列表存进 `s`，而是一个命题，表示存在一条符合规则的生成过程。这样既能证明与历史有关的不变量，也不必让棋盘对象携带所有过去着法。

可达性已经用于证明：

- 轮到黑时黑白子数相等；
- 轮到白时黑子恰好多一个；
- 可达局面不可能同时有黑方五连和白方五连。

重要边界：`CanForceWin` 也可以用于手工构造的局部局面，并不在定义里强制要求 `Reachable`。因此局部战术示例可以研究任意干净棋形；若要把它接到完整对局上，调用者还应证明该局面可达，或者明确说明只是在研究局部模型。

审查问题：结论若依赖黑白交替、子数关系或“此前无人获胜”，是否有 `Reachable` 或相应的独立前提？

## 13. `legalMove`：什么叫当前局面的合法着法

项目定义为：

```lean
legalMove s c := ¬ IsTerminal s ∧ s.board.cell c = .empty
```

也就是说，一步合法当且仅当：

1. 当前比赛尚未结束；
2. 目标格为空。

着法不另带“我要替黑方还是白方下”的参数。行棋方已经由 `s.turn` 唯一决定，`play s c` 总是放入 `s.turn` 的棋子，然后把轮次交给另一方。因此“白棋在黑方回合抢着下”不是一种可传入 `play` 的操作。

这一定义同时实现了：不能占用已有棋子、五连后不能继续、满盘后不能继续。无禁手意味着它不检查三三、四四或长连；长连在本项目中还是胜利。

审查问题：有人只证明某格为空，便说它是合法着法了吗？还必须排除当前局面已经终局。

## 14. `play`：执行一步，不负责替你证明这一步合法

```lean
play s c = ⟨s.board.place c s.turn, s.turn.other⟩
```

`play` 是一个总函数：Lean 允许对任何 `s`、`c` 计算它，即使 `c` 已被占用。原因是总函数更容易组合和化简；合法性由定理前提承担。

所以看到 `(play s c)` 时，必须向上寻找 `hlegal : legalMove s c`，除非代码明确是在构造非法输入的反例。常见模式是：

```lean
(hlegal : legalMove s c) → 某个关于 play s c 的结论
```

这与数学中先定义一个代数表达式，再在适当定义域上证明性质相似。

## 15. `IsTerminal`、`terminal` 与 `Outcome`

三者分别回答不同问题：

- `IsTerminal s : Prop`：只问“结束了吗”；
- `terminal s : Option Outcome`：实际计算结果，可能是黑胜、白胜、和棋或尚未结束；
- `Outcome`：三种结果的数据类型。

终局条件是：黑方至少五连，或白方至少五连，或棋盘已满。`terminal` 的计算顺序先看黑胜、再看白胜、最后看和棋。在可达局面中已经证明双方不会同时胜，因此这个顺序不会造成真实对局歧义。对于人工拼出的“双赢棋盘”，`terminal` 会优先返回黑胜；这也是研究任意局部棋盘时必须知道的边界。

“至少五连”意味着六连、七连等长连同样获胜。没有“恰好五连”限制。

## 16. `step`：沿某个方向移动若干格

```lean
step c d n : Option Coord
```

输入起点 `c`、方向 `d` 和整数偏移 `n`：

- 新坐标仍在 15×15 棋盘内，返回 `some q`；
- 越过边界，返回 `none`。

四个方向是横、竖、右上斜线和右下斜线。负偏移允许沿反方向走，所以不需要再列左、下、左下、左上四个重复方向。

`step_reverse` 证明：若从 `c` 沿某方向走 `n` 到 `q`，那么从 `q` 沿同一方向走 `-n` 回到 `c`。这是后续快速五连检查的重要几何事实。

## 17. 连续段、开放端与边界行为

`occupiedAt b p c d n` 表示：从 `c` 沿 `d` 偏移 `n` 的位置确实在棋盘上，而且那里是玩家 `p` 的棋子。

`consecutive b p c d length` 表示从 `c` 开始，偏移 `0, 1, ..., length - 1` 的格子全是 `p` 的棋子。

`openEnd b c d n` 表示对应偏移不仅存在，而且是空格。边界外没有坐标，所以边界外绝不是开放端。这正是“棋盘边界视为封闭”的严格实现。

例如直线活四写成：连续四个同色棋子，并且偏移 `-1` 和 `4` 都是棋盘内空格。若四子贴着棋盘边缘，缺失的一端返回 `none`，于是不能满足 `openEnd`。

## 18. 最大连续段与规范起点

`MaximalRun b p c d n` 表示：

1. 从 `c` 开始有连续 `n` 个 `p` 的棋子；
2. 前一格不是 `p` 的棋子；
3. 后一格不是 `p` 的棋子。

“不是同色棋子”可能因为那里为空、是对方棋子，或者已经越界。请注意：“最大连续段”不等于“两端开放”；边界上的三连可以是最大连续段，但不是活三。

`canonicalRunStart` 表示前一格不是同色棋子，用于规定一段连续棋子的唯一代表起点。“规范化”不是改变棋形，而是防止同一个图形从多个位置被重复计数。当前直线活三、活四本身已经要求前端为空，因此自动满足规范起点，源码还证明了规范化版本与原版本等价。

审查问题：某个集合的 `card ≥ 2` 真的是两个不同威胁，还是同一条线被不同起点重复登记？

## 19. 几何棋形、局部战术与安全条件

这三个层次必须分开：

1. 几何棋形：棋盘上某些格子是什么内容，例如“连续三子且两端为空”；
2. 局部战术：某一步之后会出现哪些可利用的后续点；
3. 安全条件：把对方所有合法应手和反击也纳入，足以推出真正的强制获胜。

`straightOpenThree`、`straightOpenFour`、`brokenOpenThree`、`jumpFour` 都是冻结的有限图形定义。断三当前只包含源码中明确列出的两类空隙位置；跳四只包含明确列出的三类空隙位置。没有写进析取式的民间叫法，不会自动被 Lean 承认。

“双活三”尤其容易偷换概念。`GeometricDoubleOpenThree` 只说明落子后出现至少两个不同的直线活三见证；它本身不排除对方已经有立即胜着，也不保证对手的某个应手不会同时拆掉威胁。项目已经有反例展示：纯几何双活三不自动等于“一步后立刻形成两个成五点”。

`SafeDoubleOpenThree` 是强语义条件：除棋形和非终局、无对手立即胜着之外，还直接要求对方每个合法防守之后目标方仍有 `CanForceWin`。因此 `safeDoubleOpenThree_forces_win` 是可靠的组合定理，但不能夸大成“已从纯双活三几何自动推导出必胜”。真正有价值的后续工作，是寻找更简单、可检查、而又足以推出这项安全条件的棋形和反击排除定理。

## 20. 三种“可赢点”为什么不能混为一谈

### 20.1 `WinningMoves`

`WinningMoves s p` 收集当前局面中玩家 `p` 可以合法落下并立即得到胜局的着法。它检查 `s.turn = p`、`legalMove` 和落子后的 `terminal`。

### 20.2 `WinningCells`

`WinningCells s p` 收集所有空格：假设把 `p` 的棋放在那里，会形成至少五连。它故意不检查当前轮次和父局面是否终局。

为什么需要这种看似较弱的集合？黑棋制造两个成五点之后，马上轮到白棋；此时黑棋的 `WinningMoves` 因为轮次不对会是空集，但两个黑棋威胁点依然客观存在。`WinningCells` 正是为了描述这种“潜在成五点”。

### 20.3 `FourExtensionCells`

`FourExtensionCells b p` 收集能让 `p` 至少形成连续四子的空格。它描述从活三走向四连的扩展点，不是立即获胜点。

三个集合可以这样记：

| 名称 | 看轮次 | 看当前是否可合法继续 | 落子后的要求 |
| --- | --- | --- | --- |
| `WinningMoves` | 是 | 是 | 正式终局且 `p` 获胜 |
| `WinningCells` | 否 | 否，只要求该格为空 | 至少五连 |
| `FourExtensionCells` | 否 | 否，只要求该格为空 | 至少四连 |

审查问题：一个证明需要的是“现在就能走的胜着”，还是“不考虑轮次的威胁格”？选错集合会导致结论过强或条件无法满足。

## 21. 策略、获胜策略与 `CanForceWin`

`Strategy p` 是一个位置函数：每当轮到 `p`、局面可达且尚未终局，它返回一枚合法着法。它回答“在这个局面我选择什么”。

但一个总策略可以在必败局面中仍返回合法棋，所以“有策略函数”不等于“有获胜策略”。`StrategyRealizes σ s hs` 进一步说明具体策略 `σ` 从 `s` 开始确实形成一棵获胜应对树。

`CanForceWin s p` 是项目最核心的博弈命题。它有三种证明方式：

1. 当前已经以 `p` 获胜；
2. 轮到 `p` 时，存在一个合法选择，其子局面仍可强制获胜；
3. 轮到对手时，对手的每一个合法选择，其子局面都仍可强制获胜。

这里“存在”与“对所有”的交替正是博弈中的主动选择和被动应对。不能只展示一条对手配合的获胜路线。

项目证明了：对可达局面而言，存在一个真正实现胜利的策略，与 `CanForceWin` 等价。证明用空格数严格下降保证递归沿棋局向前推进，不会无限下棋。

## 22. 策略树、策略证书与检查器

策略树是完整的分支说明：

- 在我方节点，只需给出一个选择；
- 在对方节点，必须给出全部合法应手；
- 叶子必须是我方真实获胜的终局。

依赖类型的 `CertificateTree` 直接把每个节点的正确性编码在类型中，数学上很清楚，但外部程序很难生成。`CompactCertificate` 则把树压平成数组，每个节点保存局面以及子节点索引，适合搜索器输出。

`checkCertificate` 是可信检查器。它逐项检查：

- 目标玩家是黑棋；
- 根索引存在，根局面等于空棋盘黑先；
- 终局叶子的结果确实是目标玩家获胜；
- 我方节点的着法合法、轮次正确、子局面匹配；
- 对方节点的列举全是合法着法，并覆盖全部合法应手；
- 每条引用都指向数组中严格靠后的节点。

最后一条同时排除自环和循环。正确性证明从数组后部向前重建真正的 `CertificateTree`，再由策略树的 soundness 得到 `CanForceWin`。

核心结论是：

```lean
theorem compact_certificate_sound (c : CompactCertificate)
    (h : checkCertificate c = true) :
    CanForceWin initialPosition .black
```

`soundness` 可译为“可靠性”或“健全性”：检查器只要接受，就绝不会把一个错误证书说成胜证。它不保证搜索器一定能找到证书，这叫完备性问题，是另一件事。

还有本地证书检查接口，它允许从任意指定局面检查小型战术证书。它没有放宽节点和边的验证，只是没有把根强制固定为空棋盘。

## 23. 可信基础：究竟需要相信什么

本项目采用“搜索器可犯错，检查器不能放错”的分工：

- 外部或纯 Lean 搜索器负责猜候选树；
- Lean 中的检查器重新核对每个节点；
- 正确性定理把检查成功连接到 `CanForceWin`；
- Lean 内核最终检查整个证明项。

所以搜索启发式、走法排序、缓存甚至外部语言实现都不必进入数学可信基础。它们可以漏掉解、运行太慢或输出坏证书；坏证书应该被拒绝，而不能导出假定理。

测试中的 `native_decide` 用于执行具体例子和回归检查。它很有价值，但核心证书 soundness 不是靠“跑过几个测试”建立的，而是通用 Lean 定理。

## 24. 搜索深度、缓存和精确局面键

当前纯 Lean 搜索器按给定 `fuel` 搜索有限深度。`fuel = 0` 时没有继续展开的预算。返回 `some tree` 表示找到了候选树；返回 `none` 可能表示这一深度没找到、分支失败或预算不足，绝不等于“已经证明该局面不能赢”。

`PositionKey` 把当前行棋方和 225 个格子的内容完整保存为键。源码证明了这种键不会把两个不同局面误认为同一局面，所以它不是可能碰撞的简短哈希。

`SearchKey` 还包含搜索深度和目标玩家，因为同一局面在不同剩余深度、不同证明目标下不能随意复用结果。`SearchMemo` 提供查询、插入以及命中/未命中的适配接口；带缓存的包装在命中时可复用预存结果，没命中时调用原搜索器，而且最后仍经过证书检查。

当前限制必须说清：缓存尚不是在递归搜索过程中自动不断填充和复用的完整状态化置换表。它主要是一个安全的外层复用接口，因此不能据此声称大规模搜索问题已经解决。

“增量维护”是未来性能方向：落一子时只更新受影响的线、威胁或键，而不是每个节点重新扫描全盘。任何增量版本都需要证明或经可信检查桥接到原始完整语义。

## 25. 回归测试、对抗性审计与证明的边界

回归测试是在已知输入上检查预期结果，防止以后修改时旧行为悄悄坏掉。正例说明系统能识别应当接受的情况，反例说明它会拒绝某些不应接受的情况。

对抗性审计更进一步：主动寻找最容易被错误定义放过的输入，例如：

- 重复落在已占格；
- 贴边棋形被误算成开放；
- 六连被活四见证重复计数；
- 几何双活三遇到对手已有立即胜着；
- 证书遗漏一个对方合法应手；
- 证书子节点局面不等于实际落子结果；
- 引用自身或更早节点，企图制造循环；
- 搜索快速判定与完整终局语义不一致。

测试只能说明列出的样例通过，不能覆盖所有 15×15 棋盘。通用定理则对其量词范围内的全部对象成立。最稳健的开发方式是：先用正反例帮助冻结语义，再为关键桥接写通用定理，最后继续用回归测试保护工程行为。

---

# 第三篇：按源码学习整个项目

不要一上来从第一行读到最后一行。下面的顺序按概念依赖安排。每一章只要求先读关键定义和定理陈述；长证明体可以第二遍再看。

## 26. 第一站：`Gomoku/Basic.lean`

### 26.1 先读什么

按以下顺序定位：

1. `Player` 和 `Player.other`；
2. `Cell`；
3. `Coord`；
4. `Board`、`Board.empty`、`Board.place`；
5. `Board.count`、`Board.emptyCount`、`Board.full`；
6. 文件中公开的 `theorem` 陈述。

第一遍可以跳过两个 `private theorem` 的证明细节。它们处理有限集合过滤和删除，是计数定理的技术支撑，不影响你先理解公开接口。

### 26.2 代表定理：`Board.place_other`

```lean
theorem place_other (b : Board) {c d : Coord}
    (h : d ≠ c) (p : Player) :
    (place b c p).cell d = b.cell d
```

逐项翻译：

- `b : Board`：任意原棋盘；
- `{c d : Coord}`：任意两个坐标；
- `h : d ≠ c`：观察点 `d` 不是落子点 `c`；
- `p : Player`：任意玩家；
- 结论：在 `c` 写入 `p` 的棋子后，`d` 处内容与原来相同。

这个定理没有要求 `c` 为空，因为它描述的是底层写操作的局部性，不是在证明一步合法棋。之后许多战术证明都用它说明“在威胁点 `m` 落子不会改变线上其他格子”。

读完后应能回答：为什么 `Board.place` 不能代替 `play`？为什么 `place_other` 不需要合法性前提？

小练习：把 `Board.place_same` 翻译成中文，并说明它与 `place_other` 合起来描述了什么。

## 27. 第二站：`Gomoku/Geometry.lean`

### 27.1 先读什么

依次读：

1. `Direction`、`dx`、`dy`；
2. `step` 与 `step_reverse`；
3. `occupiedAt`、`consecutive`、`hasRun`、`hasAtLeastFive`；
4. `openEnd`；
5. `straightOpenThree`、`straightOpenFour`；
6. `brokenOpenThree`、`jumpFour` 的每个析取分支；
7. `canonicalRunStart`、规范化棋形和 `MaximalRun`。

第一遍可以跳过 `toFin15` 和 `step_reverse` 的坐标算术证明，也可以先不逐行追踪所有 `fin_cases`。重点是把每个偏移位置画在纸上。

### 27.2 如何把模式画出来

用 `X` 表示目标方棋子，`.` 表示必须为空。以 `c` 对应最左侧 `X`：

```text
straightOpenThree : .XXX.
straightOpenFour  : .XXXX.
brokenOpenThree   : .XX.X.  或  .X.XX.
jumpFour          : .XXX.X. 或 .XX.XX. 或 .X.XXX.
```

这些图只用于辅助理解，真正定义还包含“每个格子必须在棋盘内”。因此靠边缺少一个点的图不满足开放端条件。

读完后应能回答：为什么只列四个方向？为什么边界不是开放端？“最大连续三”为什么不一定是活三？

小练习：选 `(0, 7)` 为起点、横向连续三个黑子。判断它能否成为 `MaximalRun ... 3`，又能否成为 `straightOpenThree`，并解释差别。

## 28. 第三站：`Gomoku/Rules.lean`

### 28.1 先读什么

依次读：

1. `Outcome`、`winner`；
2. `Position` 与 `Position.initial`；
3. `isTerminal` 和 `terminal`；
4. `legalMove` 与 `play`；
5. `Position.Reachable`；
6. `play_preserves_other_cells`、`play_emptyCount_lt`；
7. `reachable_count_invariant`、`reachable_not_both_winners`；
8. 文件末尾的全局短名称，例如 `legalMove`、`initialPosition`。

第一遍暂时跳过终局接口互相转换的证明细节，以及 `reachable_count_invariant` 的完整战术脚本；先读其陈述和分黑白两种轮次的证明结构。

### 28.2 代表定理：`Position.play_emptyCount_lt`

```lean
theorem play_emptyCount_lt {s : Position} {c : Coord}
    (hlegal : legalMove s c) :
    Board.emptyCount (play s c).board < Board.emptyCount s.board
```

翻译：只要在 `s` 的 `c` 处落子合法，走后棋盘的空格数严格小于走前。

真正更精确的前置定理是 `play_emptyCount_succ`：走后空格数加一恰好等于走前。这项“每步少一个空格”的事实有两个用途：证明对局最长只有 225 步；在从 `CanForceWin` 构造策略时作为递归下降尺度。

这里不能删去 `hlegal`。若在已占格上调用底层 `play`，空格数可能不减少。

### 28.3 代表定理：`Position.reachable_count_invariant`

```lean
theorem reachable_count_invariant {s : Position}
    (h : Reachable s) :
    match s.turn with
    | .black => countBlack s = countWhite s
    | .white => countBlack s = countWhite s + 1
```

按 `s.turn` 分两种情况读：

- 轮到黑棋时，两方棋子数相等；
- 轮到白棋时，黑棋比白棋多一个。

证明对 `h : Reachable s` 归纳。开局两个数都是零；每走一步，只增加当前方棋子一个，然后轮次交换。这个定理不是对所有手工拼出的 `Position` 成立，只对带有可达性证明的局面成立。

读完后应能回答：为什么 `terminal` 返回 `Option Outcome`？为什么 `play` 本身不拒绝非法输入？可达性比“棋子数正确”更强在哪里？

小练习：解释为什么“黑白棋子数满足上述关系”不能反过来保证局面可达。提示：还要考虑已经五连后继续出现的棋子。

## 29. 第四站：`Gomoku/Game.lean`

### 29.1 先读什么

依次读：

1. `Strategy`；
2. `ForceWin` 的 `terminal`、`choose`、`respond` 三个构造器；
3. `CanForceWin`；
4. `canForceWin_immediate`；
5. `StrategyRealizes`；
6. `strategyRealizes_iff_canForceWin`。

第一遍可以暂时跳过 `canonicalWinningStrategy_realizes` 的强归纳证明。只要知道它使用空格数下降，把抽象获胜树转化成一个统一的位置策略即可。

### 29.2 如何读三种获胜节点

`ForceWin target s` 不是一个计算胜负的布尔值，而是一份归纳证明：

- `terminal`：`s` 已经显示目标方获胜；
- `choose`：尚未终局且轮到目标方，给出一枚合法棋及获胜子证明；
- `respond`：尚未终局且轮到对手，为对手每枚合法棋都给出获胜子证明。

注意 `respond` 的 `children` 是一个全称函数：给它任意着法 `m` 和这步合法的证明，它必须返回相应子局面的获胜证明。这正是“无论对方怎么走”。

读完后应能回答：为什么只展示“黑走 A、白走 B、黑走 C”不能证明黑棋必胜？`Strategy` 为什么保证合法，却不自动保证获胜？

小练习：假设当前轮到黑，黑在 `m` 落子后立即得到 `blackWin`。指出 `canForceWin_immediate` 的三个前提分别是什么。

## 30. 第五站：`Gomoku/Tactics.lean`

这是概念最密集的文件，应分三遍读。

第一遍读见证集合：`openThreeWitnesses`、`openFourWitnesses`、`WinningMoves`、`WinningCells`、`FourExtensionCells`。

第二遍读通用桥接：活三产生四连扩展点、活四产生获胜点、两个不同获胜点如何在对手挡一个后仍保留另一个。

第三遍读战术包装：`SingleOpenFour`、`DoubleOpenThree`、`SafeDoubleOpenThree` 以及最终的 `..._forces_win` 定理。

第一遍不必逐行追踪大量坐标搬运证明。先为每个定义写出：输入、几何条件、轮次条件、合法性条件、结论。

### 30.1 代表定理：`straightOpenFour_black_immediate`

```lean
theorem straightOpenFour_black_immediate
    (hturn : s.turn = .black)
    (hnoterm : ¬ IsTerminal s)
    (hpattern : straightOpenFour s.board .black c d) :
    ∃ m, legalMove s m ∧
      terminal (play s m) = some .blackWin
```

翻译：若轮到黑、局面未结束，并且棋盘上从 `c` 沿 `d` 是直线活四，那么存在一枚合法着法，使黑棋立即获胜。

证明选择活四右开放端，把原来的四颗棋搬运到新棋盘上，再加上新落的一颗，组成五连。因为前提已有 `hnoterm` 且端点为空，所以该步合法。

这个定理只写黑棋是当前实现的专门版本，不应擅自引用为一个已经证明的任意玩家版本。

### 30.2 代表定理：`safeDoubleOpenThree_forces_win`

```lean
theorem safeDoubleOpenThree_forces_win
    (hsafe : SafeDoubleOpenThree s p m) :
    CanForceWin s p
```

表面上它只有一个前提，但 `hsafe` 内部已经包含：

- 当前轮到 `p`，且 `m` 合法；
- 落子产生双活三几何见证；
- 落子后仍非终局；
- 对手没有立即胜着；
- 对手任意合法应手后，`p` 都仍可强制获胜。

所以证明本身主要是把这些信息装配成一个 `choose` 节点，再装配一个 `respond` 节点。最难的内容被诚实地放在安全谓词的前提中，而不是已经从纯几何自动证明出来。

这也是审查局部战术定理的典型方法：不要只看定理名字和前提数量，必须展开前提定义，看它是否已经包含了接近结论的博弈条件。

读完后应能回答：`WinningMoves` 与 `WinningCells` 为什么都需要？纯几何双活三还缺哪类论证？为什么 `singleOpenFour_forces_win` 中“白棋无立即胜着”的前提当前其实不是最小必要前提？

小练习：把某个自然语言条件“黑棋走中心后出现横竖两个活三，而且白棋无论走哪一步，黑棋都至少还有一个立即胜点”拆成源码中可能使用的四至五个谓词。

## 31. 第六站：`Gomoku/Certificate.lean`

### 31.1 三层阅读顺序

1. 先读依赖类型 `CertificateTree` 与 `CertificateTree.sound`；
2. 再读扁平的 `CertificateNode`、`CompactCertificate`；
3. 最后读 `checkNodeAt`、`checkCertificate` 和 `compact_certificate_sound`。

第一遍可跳过数组索引的 `..._iff` 辅助定理，但要列出检查器检查的所有条件。第二遍再跟踪 `compact_reify_at` 如何从后向前重建树。

### 31.2 三种紧凑节点

```lean
inductive CertificateNode where
  | terminal (position : Position) (winner : Outcome)
  | proverMove (position : Position) (move : Coord) (child : Nat)
  | opponentMoves (position : Position)
      (children : Array (Coord × Nat))
```

每个节点自己保存局面。这样会增大证书，但检查器能独立核对：引用的子节点局面是否真的等于走棋结果。若只保存索引而不检查局面关系，搜索器就可能把一个无关的获胜叶子接到任何着法后面。

### 31.3 代表定理：`compact_certificate_sound`

```lean
theorem compact_certificate_sound (c : CompactCertificate)
    (h : checkCertificate c = true) :
    CanForceWin initialPosition .black
```

逐项读：对任意紧凑证书 `c`，只要可执行检查器返回真，就能在数学博弈语义中证明空棋盘黑棋有强制胜法。

证明链为：

```text
检查证书头部和全部节点
        ↓
从根索引重建 CertificateTree
        ↓
确认根局面就是 initialPosition
        ↓
CertificateTree.sound
        ↓
CanForceWin initialPosition Black
```

为什么严格要求子索引大于父索引？因为数组有限，沿引用只能不断向后，绝不可能回到旧节点，于是没有循环；同时可按“距数组末尾还有多少项”做强归纳。

最重要的成果边界：这个定理是条件定理。我们已经证明“任何通过检查的完整证书都可靠”，但还没有实际提供一份能从初始 15×15 局面通过检查的完整证书。因此还没有无条件的 `initial_black_wins`。

读完后应能回答：为什么对手节点必须覆盖全部合法着法？坏搜索器为什么不能破坏最终正确性？本地证书检查与全局检查有何不同？

小练习：说明以下四种坏证书分别应在哪一项被拒绝：遗漏一个白棋应手、子索引越界、子局面不匹配、终局标签写成黑胜但棋盘没有黑五连。

## 32. 第七站：`Gomoku/Search.lean`

### 32.1 先读什么

按功能分组阅读：

1. `allCoords`、坐标和索引互换；
2. `PositionKey`、`boardKey_eq_iff`、`positionKey_eq_iff`；
3. 合法候选着法与走法排序；
4. `createsFiveFast` 及正确性；
5. 一步、两步小证书生成；
6. `CandidateTree` 和有限深度搜索；
7. 候选树编译成证书并重新检查；
8. `SearchMemo` 和带缓存包装；
9. 文件末尾的回归例子。

第一遍不要试图分析性能数字，也不必追 `partial def` 的每个递归分支。先确保你知道：搜索器生成候选，检查器裁决候选。

### 32.2 代表定理：`createsFiveFast_terminal_iff`

```lean
theorem createsFiveFast_terminal_iff
    (hturn : s.turn = p)
    (hlegal : legalMove s m) :
    createsFiveFast s.board p m = true ↔
      terminal (play s m) = some (winner p)
```

`createsFiveFast` 不重新枚举棋盘上每一个五格窗口，只检查包含新落子 `m` 的四个方向、每个方向五种可能位置，最多 20 个窗口。

等价定理说明：在轮次正确且着法合法的前提下，快速判断返回真，当且仅当正式终局语义判定这一步让 `p` 获胜。这里既有 soundness（报真时确实赢），也有 completeness（确实因这步获胜时不会漏报）。

合法性前提非常关键：它保证父局面尚无赢家，所以子局面的胜利确实是由新落子造成的；也保证 `play` 没有覆盖旧棋。

### 32.3 当前缓存到什么程度

当前已有无损 `PositionKey`、包含深度和目标方的 `SearchKey`、备忘录条目、查询/插入定理、命中与未命中的缓存包装，以及“包装若产出并通过本地检查，则 `CanForceWin`”的正确性桥。

但递归搜索函数本身仍是原始搜索；包装不会在每个递归子调用后自动把新结果写回并传播更新后的表。因此它是安全的缓存适配层，不是足以承担全盘求解的成熟置换表。后续若重写成真正带状态的递归搜索，仍应让最终结果经过现有证书检查器。

读完后应能回答：为什么搜索 `none` 不是数学上的“不存在胜法”？为什么局面键必须包括轮次？为什么搜索结果仍要检查？

小练习：解释为什么走法排序可以把立即胜着放前面，却不能未经证明直接删掉所有“离已有棋子较远”的合法着法。

## 33. 第八站：`Examples.lean` 与 `Adversarial.lean`

`Examples.lean` 是基础正例和规则回归。适合第一次动手，因为其中许多 `example` 短小、具体、能直接执行。重点观察：初始轮次、中心落子、横向和斜向活四、边界反例、长连胜利。

`Adversarial.lean` 是主动挑刺的测试场。它不只是证明系统“会接受正确例子”，还构造最可能暴露错误语义的情况：非法占点、断三和跳四、边界封闭、纯几何双活三的不足、对手反击、遗漏分支、错误引用、错误子局面、错误终局标签和循环证书。

阅读时为每个例子贴一个标签：

- 通用定理还是具体棋盘；
- 正例还是反例；
- 规则、几何、战术、证书还是搜索；
- 用逻辑证明还是用具体计算。

不要因为 `example` 在 Lean 中通过，就说“所有这类棋形都已证明”。一个无自由变量的 `example` 通常只证明这一张具体棋盘。

读完后应能回答：回归例子与通用定理的量词范围有何差别？为什么负向测试对冻结自然语言语义很重要？

小练习：在 `Adversarial.lean` 中找到 `crossForkPosition` 相关例子，用中文说明它反驳了哪一个过强说法。

## 34. 七个代表定理的快速复盘表

| 定理 | 前提的核心 | 结论 | 不能夸大成什么 |
| --- | --- | --- | --- |
| `Board.place_other` | 两坐标不同 | 其他格不变 | `place` 本身是合法落子 |
| `play_emptyCount_lt` | 着法合法 | 空格严格减少 | 任意调用 `play` 都减少 |
| `reachable_count_invariant` | 局面可达 | 子数与轮次匹配 | 子数匹配就一定可达 |
| `straightOpenFour_black_immediate` | 黑方回合、非终局、直线活四 | 存在黑方立即胜着 | 任意叫作“四”的图形都必胜 |
| `safeDoubleOpenThree_forces_win` | 完整安全谓词 | 可强制获胜 | 纯几何双活三自动必胜 |
| `compact_certificate_sound` | 全局证书检查为真 | 初始黑棋可强制获胜 | 已经拥有完整证书 |
| `createsFiveFast_terminal_iff` | 轮次正确、着法合法 | 快速判定等价正式胜局 | 快速搜索整体已完备 |

---

# 第四篇：怎样实际掌握并接手项目

## 35. 五周学习计划总览

建议每周安排两到三次，每次 45–90 分钟。每次只做一个小目标，并留下自己的中文笔记。不要以“读完多少行”为标准，而以“能否不看文档回答验收问题”为标准。

| 周次 | 核心目标 | 主要源码 | 本周产出 |
| --- | --- | --- | --- |
| 第 1 周 | 会运行项目、读基本定义 | `Basic`、`Examples` | 自己运行三个例子并解释类型 |
| 第 2 周 | 理解几何与规则 | `Geometry`、`Rules` | 画出模式表，解释可达性和终局 |
| 第 3 周 | 理解“必胜”的量词结构 | `Game`、`Tactics` | 拆解两个战术定理的全部前提 |
| 第 4 周 | 理解证书和可信边界 | `Certificate`、`Adversarial` | 手工检查一棵小证书并找坏证书错误 |
| 第 5 周 | 理解搜索与团队管理 | `Search`、项目说明 | 审查一个模拟 PR，拟定下一阶段任务 |

若某周验收没有通过，就多用一次短学习时段，不必机械进入下一周。

## 36. 第 1 周：能打开、能运行、能读声明

### 第一次（45–60 分钟）

1. 阅读本文第 2–9 节；
2. 打开 `Gomoku/Basic.lean`；
3. 找到 `Player`、`Cell`、`Coord`、`Board`；
4. 用中文写下每个类型有哪些值；
5. 在项目目录运行：

```powershell
lake build
```

成功的含义是：项目所有被构建目标能通过 Lean 检查。警告不等于错误，但应阅读警告内容；若命令失败，先看输出中第一条真正的 `error`。

### 第二次（45–75 分钟）

新建一个仅供自己练习的 Lean 文件，例如 `LearningScratch.lean`，内容可从下面开始：

```lean
import Gomoku

open Gomoku

#check Player
#check Board.place
#check legalMove
#check Position.play_emptyCount_lt

example : Player.other .black = .white := by
  rfl

example : initialPosition.turn = .black := by
  rfl
```

然后运行：

```powershell
lake env lean LearningScratch.lean
```

练习文件不属于正式成果，除非团队决定保留，否则不要提交。你可以把某个 `example` 的 `.black` 改成 `.white`，观察错误，再改回来。故意制造一个小错误，是学习 Lean 报错最快的方法之一。

### 本周验收

你应能解释：

- `#check` 只查询某个名字的类型，不修改项目；
- `lake env lean 文件名` 检查一个文件；
- `lake build` 检查工程构建目标；
- `rfl` 为什么能证明“黑棋的对手是白棋”；
- `Board` 为什么是一张棋盘照片，而不是完整对局。

## 37. 第 2 周：把棋盘规则翻译成数学条件

### 第一次：几何

阅读第 16–19、27 节，然后打开 `Geometry.lean`。在方格纸上画出四个方向和所有冻结模式。对每个模式标注：

- 哪些位置必须是己方棋子；
- 哪些位置必须为空；
- 哪些位置若越界便使模式失败；
- 起点是否唯一。

选择三个例子手算 `step`：普通位置正偏移、负偏移、越界。再在 `Examples.lean` 找到边界活四反例，对照定义解释为什么失败。

### 第二次：规则

阅读第 12–15、28 节。把一次合法对局写成：

```text
初始局面
  --合法落子--> 局面 1
  --合法落子--> 局面 2
  --合法落子--> ...
```

然后逐项解释 `Reachable.step`、`legalMove`、`play`。特别检查：形成五连后为什么没有下一条合法箭头。

### 第三次：小练习

在练习文件中加入：

```lean
#check step
#check straightOpenThree
#check Position.Reachable.step
#check Position.reachable_count_invariant

example : legalMove initialPosition (7, 7) := by
  native_decide

example : (play initialPosition (7, 7)).turn = .white := by
  rfl
```

`native_decide` 在这里用于具体学习例子，不意味着你掌握了通用证明，也不应拿来替换核心证书可靠性证明。

### 本周验收

你应能回答：

- 越界为什么返回 `none` 而不是一个假坐标；
- 边界如何自动成为封闭端；
- 六连为什么也是胜利；
- 可达局面与“看上去合理的手工棋盘”有什么差别；
- 为什么合法着法既检查空格又检查未终局。

## 38. 第 3 周：掌握强制获胜和局部战术

### 第一次：只研究量词

阅读 `ForceWin`，不用看证明体。把三种构造器各画一个节点。用 `∃` 标记我方节点，用 `∀` 标记对方节点。

自己造一个错误论证：“黑走这里，假设白挡左边，黑走右边获胜。”指出它只覆盖了白棋一个选择，没有覆盖其他合法应手，因此不能建立 `respond`。

### 第二次：见证集合

为 `WinningMoves`、`WinningCells`、`FourExtensionCells` 各写一个自然语言定义和一个使用场景。打开 `Tactics.lean` 对照源码，确认有没有漏掉轮次、终局或空格条件。

### 第三次：拆定理

逐层展开：

```text
safeDoubleOpenThree_forces_win
  需要 SafeDoubleOpenThree
    需要 DoubleOpenThree
      需要 GeometricDoubleOpenThree
      还需要轮次与合法落子
    还需要落子后非终局
    还需要对手无立即胜着
    还需要每个防守后仍 CanForceWin
```

然后对比 `straightOpenFour_black_immediate`：后者从明确几何和规则前提直接构造立即胜着；前者的安全谓词已经包含多步博弈要求。两者证明强度和未来研究价值不同。

### 本周验收

你应能回答：

- “能赢的一条变化”与“强制获胜”差在哪个量词；
- 活三为什么不是立即胜利；
- 两个活三见证为什么不自动等于两个立即胜点；
- 如何识别一个谓词是否已把结论藏进前提；
- 当前哪些局部结论是几何直接推出，哪些依赖语义安全条件。

## 39. 第 4 周：掌握证书、检查器和对抗性证伪

### 第一次：手画一棵证书树

画一个“黑有立即胜着”的两节点树：根为黑方选择节点，子节点为黑胜终局。再画一个轮到白的分支节点，假设白只有两个合法着法，就必须画两个子节点。

对每条边检查：着法合法吗？子棋盘真是落子结果吗？子索引在范围内吗？是否严格向后？

### 第二次：读检查器

阅读 `CertificateNode`、`checkNodeAt` 的三个 `..._iff` 定理、`checkCertificate` 和 `compact_certificate_sound`。不要求逐行理解数组证明，但要能复述检查清单和 soundness 证明链。

### 第三次：对抗性审计

阅读 `Adversarial.lean` 中以下四类坏证书：

- `missingReplyCertificate`；
- `badReferenceCertificate`；
- `badChildPositionCertificate`；
- `badTerminalLabelCertificate`。

在看检查结果前先猜它们为什么应被拒绝，再对照 `checkLocalCertificate ... = false`。随后阅读 `cycleCertificate`，理解严格向后引用怎样排除循环。

### 本周验收

你应能回答：

- 搜索器输出一个证书为何还不够；
- 检查器漏查“全部应手覆盖”会造成什么漏洞；
- soundness 与“搜索一定找得到”有何区别；
- 为什么当前定理还不能写成无条件的 `initial_black_wins`；
- 对抗性测试能发现什么，又不能替代什么。

## 40. 第 5 周：理解搜索并开始指导团队

### 第一次：搜索边界

阅读第 24、32 节。跟踪一条流程：

```text
候选着法 → 候选树 → 紧凑证书 → 检查器 → CanForceWin
```

为每个箭头标注：是可犯错的搜索步骤，还是已经证明的可信桥接。确认搜索器返回 `none` 时流程在何处中止，以及为什么不能推出否定结论。

### 第二次：性能与正确性分离

比较下面三种改动：

1. 把立即胜着排在候选列表前面；
2. 删除离已有棋子很远的合法着法；
3. 用精确局面键复用已经搜索过的结果。

第一项只改变顺序；第二项改变覆盖集合，若无完备性证明可能漏掉胜法；第三项必须保证键、深度、目标方和缓存结果语义都匹配。最终候选仍经证书检查，可以保护 soundness，但漏搜仍会影响能否找到答案。

### 第三次：模拟审查

拿本文第 50 节任务模板，为“增加一种新的断三模式”写一份任务说明。至少写清图形、边界、正反例、希望得到的通用定理和构建命令。然后用第 47 节清单自审一次。

### 本周验收

你应能完成三件事：

- 向完全不懂 Lean 的人准确介绍当前成果和未完成部分；
- 阅读一个新定理的变量、前提、结论，判断是否夸大；
- 给队友一个有定义、有反例、有证明目标、有验收命令的任务。

## 41. 如何使用 `#check` 和编辑器探索 API

不知道某个名字怎么用时，不要猜。可以在练习文件写：

```lean
#check Gomoku.Position.play_emptyCount_lt
#check @Gomoku.safeDoubleOpenThree_forces_win
#print Gomoku.SafeDoubleOpenThree
#print Gomoku.ForceWin
```

- `#check name` 显示类型；
- `#check @name` 尽量显示隐式参数；
- `#print name` 显示定义体或定理信息；
- 在支持 Lean 的编辑器中，把鼠标停在名字上也能看类型。

若 Lean 报“unknown identifier”，先检查：是否 `import Gomoku` 或导入了对应模块；名字是否在 `Gomoku` 命名空间；大小写是否完全一致。

阅读长类型时使用固定顺序：

1. 找冒号后的最终结论；
2. 从左到右列出每个括号前提；
3. 标记隐式变量的类型；
4. 展开你不理解的自定义谓词；
5. 最后才看 `:= by` 后面的证明脚本。

## 42. 怎样把自然语言棋形写成严格条件

以“黑棋走 `m` 形成双活三，所以黑棋赢”为例，不应立即写定理。先完成下面七步。

### 第一步：冻结棋盘范围和规则

确认是 15×15、无禁手、至少五连即胜、边界封闭、落子后立即终局。

### 第二步：画出有限模式

明确每个相关格是黑子、白子、空格还是任意。对断点和开放端逐格编号。不要用“类似的形状也算”作为正式定义。

### 第三步：确定对象层级

- 只描述静态形状，用 `Board`；
- 涉及轮次、终局、合法落子，用 `Position`；
- 涉及真实历史性质，再加 `Reachable`。

### 第四步：明确不同性的含义

“两个活三”是两个不同方向、不同规范起点、不同扩展点，还是只要两个模式见证？把不同性写成坐标或见证不等式，防止重复计数。

### 第五步：枚举对手反击

至少检查：对手是否已有立即胜着；对手能否在防守同时形成四或成五；一枚防守是否能同时破坏两个威胁；落子是否意外使局面先终结。

### 第六步：决定希望证明的强度

可能的结论按由弱到强排列：

1. 产生一个四连扩展点；
2. 产生一个成五点；
3. 产生两个不同成五点；
4. 对手每种合法应手后仍有立即胜着；
5. `CanForceWin s p`。

先证明足以支持下一步的最弱结论，避免把难题藏进定义。

### 第七步：正例、反例、通用定理三件套

至少准备：一个内部正例、一个边界反例、一个对手反击反例，以及对任意满足条件的棋盘成立的通用定理。具体例子帮助检查定义，通用定理承担真正证明。

## 43. 怎样判断定理够不够强、有没有偷换概念

审查任何战术结论时，做以下检查：

1. **看量词范围**：是任意局面，还是一个写死的棋盘？
2. **展开命名前提**：`Safe...` 里面是否已经直接要求 `CanForceWin`？
3. **检查轮次**：棋形存在时是否真的轮到目标方？
4. **检查终局**：父局面是否已经结束？
5. **检查合法性**：目标点为空不等于完整合法；是否排除终局？
6. **检查对手全称分支**：是每个合法应手，还是只讨论一个常见防守？
7. **检查见证唯一性**：两个见证是否可能代表同一威胁？
8. **检查结论层级**：形成四、形成五和强制获胜不能混用。
9. **检查可达性要求**：若声称来自正式开局，是否建立连接？
10. **检查计算边界**：搜索没找到是否被错误说成不存在？

一个定理“够强”是相对于用途而言。例如 `straightOpenThree_has_fourExtension` 足以说明活三能发展成四，却不足以说明必胜；这不是定理错误，而是不能在说明文档中把它升级描述。相反，某个 `Safe...` 定理结论很强，但若安全条件已经包含后续每分支 `CanForceWin`，它主要是组合接口，不代表最困难的几何分析已经完成。

## 44. 五类成果标签

以后在任务表、README 和口头汇报中，建议给每项成果标记以下一种：

| 标签 | 含义 | 例子 |
| --- | --- | --- |
| 已证明通用定理 | 对声明中所有对象成立，Lean 已检查 | 合法落子严格减少空格 |
| 已验证具体例子 | 只对写出的棋盘或数据成立 | 某张边界棋盘不是活三 |
| 已完成接口 | 数据结构与正确连接存在，但未解决大规模实例 | 紧凑证书检查接口 |
| 实验性优化 | 旨在加速，仍需性能或正确性评估 | 缓存适配、走法排序 |
| 最终未完成目标 | 还缺必要实例或证明 | `initial_black_wins` |

同一个模块可以同时包含多个标签。例如证书模块的 soundness 是已证明通用定理，而“完整 15×15 证书”仍是未完成目标。不能因为前者完成就把后者标记完成。

---

# 第五篇：怎样指导团队和规划后续工作

## 45. 一个合格任务必须包含什么

以后不要只给队友一句“形式化一下活三”或“优化一下搜索”。这种任务没有统一语义，也无法客观验收。每个任务至少包含七项。

### 45.1 自然语言目标

用两三句话说明要解决的实际问题，以及它属于规则、几何、战术、证书还是搜索。

### 45.2 严格定义草案

列出输入对象和每项条件。新棋形要画模式图，说明边界、开放端、断点、不同见证如何计数。若定义仍有两种合理解释，先讨论冻结，不能让实现者自行猜测。

### 45.3 前提与结论

把希望得到的定理拆成：任意变量、规则前提、几何前提、安全前提、最终结论。特别注明是否要求 `Reachable`、当前轮次、非终局和合法落子。

### 45.4 正例与反例

至少包括：普通内部正例、边界反例、与相近棋形的区分例子、对手反击例子。证书任务还应包括遗漏分支、错误引用或错误标签等坏输入。

### 45.5 通用定理

不能只提交一张具体棋盘通过 `native_decide`。任务要说明哪个有变量的命名定理是主要成果，以及具体例子如何保护它的预期语义。

### 45.6 允许修改的范围

列出预计修改的模块。若需要改底层公共定义，必须说明对上层接口的影响，避免为一个局部任务顺手重构全项目。

### 45.7 验收命令和成果说明

至少运行相关文件检查和 `lake build`。提交说明必须按第 44 节标签区分：通用定理、具体测试、接口、实验优化和未完成目标。

## 46. 新内容应该放在哪个模块

| 新内容 | 首选模块 | 选择理由 |
| --- | --- | --- |
| 玩家、格子、棋盘底层更新 | `Basic.lean` | 不依赖对局和几何 |
| 新方向操作、线段、棋形模式 | `Geometry.lean` | 只描述静态空间关系 |
| 终局、合法性、可达性、不变量 | `Rules.lean` | 定义正式对局规则 |
| 强制获胜、策略等价性 | `Game.lean` | 统一博弈量词语义 |
| 活三/四、威胁集合、局部胜势 | `Tactics.lean` | 从棋形连接到博弈结论 |
| 证书格式、检查器、可靠性 | `Certificate.lean` | 可信验证边界 |
| 候选生成、排序、缓存、外部接口 | `Search.lean` | 可替换且可犯错的求解层 |
| 基础正例 | `Examples.lean` | 易读的功能演示和回归 |
| 反例、坏证书、语义攻击 | `Adversarial.lean` | 集中保存挑刺测试 |

判断方法是看“这个定义需要依赖什么”，而不是只看名字。例如活四的静态形状属于 `Geometry`，活四产生立即胜着的定理属于 `Tactics`；证书生成算法属于 `Search`，证书检查则属于 `Certificate`。

若新文件需要让其他模块使用，还要检查顶层 `Gomoku.lean` 是否导入。不要制造循环导入，例如让 `Geometry` 反过来依赖 `Tactics`。

## 47. 审查队友 PR 的清单

### 47.1 先看需求是否一致

- 定义对应的是团队已经冻结的自然语言概念吗？
- 棋盘大小、禁手、长连和边界约定是否一致？
- PR 有没有顺便改变未获授权的公共语义？

### 47.2 再看定理陈述

- 变量和量词范围是什么？
- 是否需要轮次、非终局、合法落子或可达性？
- 命名前提展开后有没有把结论藏进去？
- 结论是棋形、立即胜着还是 `CanForceWin`？
- 是否只证明黑棋，却在说明中写成双方通用？

### 47.3 检查正反例

- 有普通正例吗？
- 有边界、占用格和相近棋形反例吗？
- 有对手立即反击或同时防守的例子吗？
- 若修改检查器，旧的所有坏证书仍被拒绝吗？

### 47.4 检查证明与计算边界

- 核心通用定理由 Lean 证明，还是只有一个具体 `native_decide`？
- 搜索返回 `none` 是否被误解为否定证明？
- 启发式是否删除了合法分支？若删除，完备性依据是什么？
- 缓存键是否含轮次、目标方和相关深度？
- 搜索成功结果是否仍经过证书检查？

### 47.5 检查工程质量

- 改动是否集中在合理模块？
- 是否意外覆盖或删除别人的修改？
- 新名字是否表达实际强度，还是过度使用 `forces_win`？
- 是否添加必要的简短注释，而没有用注释代替证明？
- `lake build` 是否成功？是否有新的重要 linter 警告？
- README、项目指南或审计报告是否需要同步？

### 47.6 审查结论怎么写

不要只回复“看起来可以”。应写成：

```text
语义：与冻结定义一致 / 有以下歧义。
证明：通用定理覆盖……；仍缺……。
反例：已覆盖……；建议增加……。
构建：运行了……，结果……。
成果标签：已证明通用定理 / 具体测试 / 接口 / 实验优化。
```

## 48. 对抗性证伪应该成为固定流程

你此前提出“给已经做好的内容挑刺”是必要的，而且不应只做一次。形式化证明能保证“从当前定义推出当前定理”，却不能自动保证定义就是人们想表达的规则。对抗性证伪专门攻击定义和接口之间的错位。

### 48.1 每次新增棋形时

主动尝试这些变异：

- 把一个必要空格换成对方棋子；
- 把图形平移到边界；
- 延长成五连或长连，看它是否被错误计为活四；
- 移动断点，测试未冻结变体是否意外进入；
- 用两个不同起点表示同一条线，检查重复计数；
- 让落子点预先被占用；
- 让当前轮次属于另一方；
- 让父局面已经终局。

### 48.2 每次新增强制胜定理时

尝试构造：

- 对手已经有立即胜着；
- 对手防守的同时成五；
- 一步同时封住两个看似独立的威胁；
- 两个见证共享同一个真正获胜点；
- 局面不可达但恰好满足局部棋形；
- 结论只在对手配合的单一路线成立。

### 48.3 每次修改证书或搜索时

尝试：空证书、根越界、自引用、向前/向后方向错误、重复应手、遗漏应手、非法着法、错误子局面、错误胜负标签、错误目标玩家、错误初始根、伪造缓存命中。

### 48.4 变异测试

更严格的做法是临时故意破坏一行定义或检查条件，确认至少一个测试会失败。例如临时去掉“全部合法应手覆盖”，那么 `missingReplyCertificate` 应暴露问题。此类破坏只用于本地审计，确认后立即恢复，不得提交。

对抗性测试没有证明“再无漏洞”，但它能显著提高语义错误被发现的概率。关键接口仍需通用正确性定理。

## 49. 什么情况下可以说“已经证明”

可以说“已经证明 X”，必须同时满足：

1. `X` 被写成明确的 Lean 命题；
2. 证明中没有 `sorry`、未声明公理或把目标直接作为假设；
3. 相关文件和工程构建成功；
4. 自然语言中的 `X` 与 Lean 命题的前提、量词和范围一致；
5. 若依赖证书，实际证书已经存在并通过相应检查器；
6. 说明没有把具体例子、搜索日志或条件定理说成无条件结论。

当前可准确说：

- 已证明合法落子只改变落子格，并严格减少一个空位；
- 已证明可达局面的轮次/棋子数不变量和不会双方同时胜；
- 已证明若黑方回合存在冻结定义的直线活四且未终局，则有立即胜着；
- 已证明若紧凑全局证书通过检查，则初始局面黑棋可强制获胜；
- 已证明若带缓存搜索包装产出通过本地检查的证书，则对应局面可强制获胜。

当前不能说：

- 已证明所有通常所称的活三或活四性质；
- 已从纯几何双活三证明必胜；
- 已实现可行的完整 15×15 求解器；
- 已得到完整策略证书；
- 已证明 `initial_black_wins`。

## 50. 可直接复制的任务模板

````markdown
# 任务名称

## 背景和目的
说明它解决哪个实际问题，属于哪个模块。

## 冻结语义
- 棋盘和规则约定：
- 输入对象：
- 模式图或数据格式：
- 边界行为：
- 明确不包含的变体：

## 需要实现的定义
列出建议名称、输入和中文含义。

## 需要证明的通用定理
```lean
theorem proposed_name ... : ...
```
逐项说明前提和结论。若声明仅是草案，在实现前先确认。

## 必须添加的例子
- 普通正例：
- 边界反例：
- 相近概念反例：
- 对手反击或坏输入：

## 修改范围
- 预计文件：
- 不应修改：
- 若必须改公共接口，先说明影响：

## 验收
- [ ] 无 `sorry`
- [ ] 通用定理存在，不只有具体计算
- [ ] 正反例通过
- [ ] `lake env lean Gomoku/相关文件.lean`
- [ ] `lake build`
- [ ] 文档准确区分已证明与未完成

## 交付说明
列出文件、核心定义/定理、运行命令、剩余限制和成果标签。
````

## 51. 和基于旧版本工作的队友怎样合并

如果队友从当前目标开始前的旧项目继续开发，不要让两个人依次直接覆盖同一分支。比较稳妥的流程是：

1. 先把你认可的最新基线推到公开 GitHub 仓库；本地两份 `LOCAL_*.md` 不提交；
2. 让队友确认自己改了哪些文件、增加了哪些定义和定理，并在自己的分支提交；
3. 队友先获取最新基线，再把自己的分支变基或合并到最新基线上；
4. 有冲突时逐段比较语义，不能机械选择“全部用我的”或“全部用他的”；
5. 队友把分支推送到 GitHub，创建 Pull Request，而不是直接覆盖主协作分支；
6. 按第 47 节审查，运行 `lake build`，确认文档状态没有倒退；
7. 合并后双方再获取同一个新基线，之后各自从新分支接任务。

因此，队友应该 push，但应 push 到自己的分支并通过 PR 展示改动。即使你暂时不知道他做了什么，也不要让他先直接合并。先得到可查看的提交和差异，才能判断是完整吸收、挑选部分提交，还是重做接口连接。

如果他的改动碰到 `Rules`、`Game`、`Certificate` 等公共语义，审查优先级高于搜索性能代码，因为底层定义变化可能影响所有上层证明。若只是新增独立例子或搜索启发式，通常更容易隔离合并。

## 52. 接下来工作的优先级

### 优先级 0：固定基线和持续验证

- 确认仓库中的源码、项目说明和审计报告同步；
- 保持 `lake build` 通过；
- 清理不应提交的临时 `.lean`、`.olean` 和本地学习文档；
- 为每个新 PR 记录实际运行过的验证命令。

### 优先级 1：再做一轮独立语义审计

- 让未参与原实现的人按规则逐项审查定义；
- 为规则、几何、战术和证书各设计新的反例；
- 对关键检查条件做本地变异测试；
- 把发现记录到 `AUDIT_REPORT.md`，修复后保留回归例子。

这一项不是因为当前成果“不可信”，而是形式化项目必须同时审查证明正确性与定义准确性。独立审查尤其能发现原作者已经习惯而忽略的假设。

### 优先级 2：把局部战术分成难度阶梯

建议按以下次序增加通用结果：

1. 一步成五与各种四连填点；
2. 两个独立即胜点；
3. 对手只有强制防守、下一步仍立即胜；
4. 明确模式的连续强迫四序列；
5. 几何活三到可验证防守后威胁的充分条件；
6. 多回合威胁空间小局面证书。

每一层都先冻结模式和反例，再连接到 `CanForceWin`。不要直接给所有复杂棋形一个含糊的“必胜”名字。

### 优先级 3：建立搜索性能基线

- 选一组从立即胜到 2、4、6 层的固定小局面；
- 记录节点数、候选数、时间、证书节点数和检查时间；
- 分开测量生成候选与 Lean 检查；
- 确保优化前后产出的成功证书都通过同一检查器。

没有基线就无法判断缓存或增量计算是否真正有效，也容易为了一个样例做出过拟合优化。

### 优先级 4：实现真正的递归置换表和增量状态

- 让递归搜索显式传入并返回更新后的缓存；
- 区分“已找到胜树”“这一深度失败”“尚未计算”；
- 使用完整局面、目标方和剩余深度作为语义键；
- 增量维护空点、最后落点、终局和立即威胁；
- 为快速更新写与参考定义一致的桥接定理或差分测试；
- 无论缓存内容如何，成功结果继续通过证书检查器。

### 优先级 5：设计可扩展的外部搜索和证书格式

纯 Lean 搜索适合验证小闭环，不一定适合完整求解。外部高性能搜索器可以使用并行、成熟内存结构和威胁空间搜索。需要提前研究：

- 完整策略树或有向无环图的规模；
- 共享相同子局面的证书表示；
- 分块生成、存储和导入；
- Lean 检查时的内存和时间；
- 对称性只用于搜索，还是需要可信压缩证明；
- 如何生成可复现的版本、参数和结果日志。

### 优先级 6：最终全局闭环

只有实际获得从空棋盘开始的完整黑胜证书后，才进入：导入证书、运行全局检查、应用 `compact_certificate_sound`、声明 `initial_black_wins`。这可能需要很长的独立研究，不应给出虚假时间保证。

## 53. 每个里程碑怎样汇报

建议固定使用四段式：

```text
本次完成：精确列出定义、通用定理和具体测试。
验证方式：列出实际运行的命令和结果。
准确边界：说明不能由本成果推出什么。
下一阻碍：指出语义、证明、性能、证书规模或外部资料中的主要问题。
```

例如缓存适配层的正确汇报是：“已加入包含深度、目标方和完整局面的键；命中结果仍经本地证书检查，并有 soundness 定理。递归搜索尚未携带并更新缓存，因此目前没有证明能显著提升深搜规模。”

这种汇报方式能让你即使不熟悉每行 Lean，也能通过前提、结论、验证和边界判断工作是否真实推进。

---

# 附录

## 附录 A：中英术语对照表

| 源码/英文 | 建议中文 | 最短直觉 | 不能误解为 |
| --- | --- | --- | --- |
| `Player` | 玩家/行棋方 | 黑或白 | 某个具体策略 |
| `Cell` | 格子内容 | 空、黑子或白子 | 坐标 |
| `Coord` | 坐标 | 两个 0–14 的数 | 会自动判断着法合法 |
| `Board` | 棋盘 | 坐标到格子内容的函数 | 完整对局局面 |
| `Position` | 局面 | 棋盘加当前轮次 | 自动可达的局面 |
| `legalMove` | 合法着法 | 未终局且目标格为空 | 只要空格就合法 |
| `play` | 执行落子 | 写入当前方棋子并换手 | 自动拒绝非法输入 |
| `Reachable` | 可达 | 能从初始局面合法走到 | 棋子数看起来合理 |
| `Outcome` | 对局结果 | 黑胜、白胜或和棋 | 是否终局的真假值 |
| `IsTerminal` | 终局命题 | 比赛是否已结束 | 具体结果数据 |
| `terminal` | 终局计算 | 返回结果或 `none` | 搜索失败 |
| `Direction` | 方向 | 横、竖、两条斜线 | 八个重复方向 |
| `step` | 有界移动 | 沿线走若干格，越界失败 | 一定得到坐标 |
| `consecutive` | 连续同色段 | 指定长度全是同色 | 已经最大或两端开放 |
| `hasRun` | 存在连续段 | 某处某方向有指定长度 | 恰好这么长 |
| `hasAtLeastFive` | 至少五连 | 存在连续五格同色 | 恰好五连 |
| `openEnd` | 开放端 | 对应位置在盘内且为空 | 越界也算空 |
| `MaximalRun` | 最大连续段 | 两端不能再接同色子 | 两端都是空格 |
| `canonicalRunStart` | 规范起点 | 为一段棋指定唯一代表 | 新的棋形条件 |
| pattern witness | 模式见证 | 一个起点与方向 | 一个必然不同的胜点 |
| `WinningMoves` | 当前立即胜着集合 | 现在合法走下就胜 | 不考虑轮次的威胁点 |
| `WinningCells` | 成五点集合 | 假放一子即可成五 | 现在一定能合法走 |
| `FourExtensionCells` | 四连扩展点 | 假放一子至少成四 | 立即胜点 |
| local tactic | 局部战术 | 从局部棋形得到有限步结论 | 完整开局策略 |
| `Strategy` | 策略函数 | 每个相关局面选一枚合法棋 | 自动保证获胜 |
| `ForceWin` | 强制获胜证明结构 | 我方选一个，对手覆盖全部 | 一条对手配合路线 |
| `CanForceWin` | 可强制获胜 | 存在对抗所有应手的胜法 | 搜索器说可能赢 |
| `StrategyRealizes` | 策略实现胜利 | 具体策略确实生成胜树 | 只保证每步合法 |
| certificate | 策略证书 | 可核验的完整应对说明书 | 搜索日志或棋谱主线 |
| checker | 检查器 | 逐项验证证书 | 自动搜索全部胜法 |
| soundness | 可靠性/健全性 | 接受的结果一定正确 | 正确结果一定找得到 |
| completeness | 完备性 | 所有目标结果都不会漏掉 | 本项目检查器 soundness |
| trusted base | 可信基础 | 最终正确性依赖的部分 | 所有参与生成的程序 |
| candidate | 候选 | 尚待检查的猜测 | 已证明对象 |
| `fuel` | 搜索深度预算 | 还能展开多少层 | 棋局一定只剩多少步 |
| cache/memo | 缓存/备忘录 | 复用相同查询结果 | 自动可信的证明库 |
| transposition table | 置换表 | 记录重复局面 | 棋盘对称性本身 |
| `PositionKey` | 精确局面键 | 轮次加 225 格内容 | 可能碰撞的短哈希 |
| regression test | 回归测试 | 防止已知行为被改坏 | 对所有输入的证明 |
| adversarial audit | 对抗性审计 | 主动构造刁钻反例 | 已经证明没有任何漏洞 |
| invariant | 不变量 | 所有合法演化都保持的性质 | 对任意手工对象都成立 |

## 附录 B：常见误解与纠正

| 常见说法 | 问题 | 准确说法 |
| --- | --- | --- |
| “`play` 能保证合法落子” | 它对非法输入也能计算 | 先证明 `legalMove`，再使用 `play` 的规则定理 |
| “目标格为空，所以能下” | 父局面可能已终局 | 合法性还要求 `¬ IsTerminal s` |
| “棋子数正确，所以局面可达” | 可能在获胜后又继续下，或有其他历史矛盾 | 可达性需要由初始局面和合法步骤归纳构造 |
| “最大连续三就是活三” | 两端可能被堵或越界 | 活三还要求两端都是盘内空格 |
| “棋盘外也没有棋，所以算开放” | 开放要求存在一个盘内空格 | `step = none` 时 `openEnd` 不成立 |
| “有四个连续棋子就叫活四” | 端点可能被堵，或已经属于更长段 | 冻结定义要求连续四且两端开放 |
| “活三下一步能成四，所以活三必胜” | 对手可以防守或反击 | 需证明每个合法应手后仍保留强制胜法 |
| “两个活三就是两个胜点” | 活三的下一步通常只形成四，不是五 | 分开使用活三见证、四连扩展点和成五点 |
| “`SafeDoubleOpenThree` 已从纯棋形证明安全” | 安全谓词直接包含防守后 `CanForceWin` | 已证明安全条件足以组合成胜树，几何充分性仍待研究 |
| “`Strategy` 就是获胜策略” | 任意非终局可达局面总有合法选择 | 还需 `StrategyRealizes` 证明它对抗所有应手获胜 |
| “搜索器找到一条赢法就够了” | 对手可能选择另一分支 | 对手节点必须覆盖每枚合法着法 |
| “搜索返回 `none`，所以不能赢” | 也可能深度不足或搜索没找到 | `none` 只是这次搜索没有产出证书 |
| “检查器通过几个测试，所以可靠” | 测试只覆盖有限样例 | 可靠性来自 `compact_certificate_sound` 的通用证明 |
| “检查器 sound，所以一定能找到全局证书” | soundness 不提供搜索完备性 | 它只保证通过的证书正确 |
| “有 `compact_certificate_sound`，黑棋必胜已证” | 定理还需要实际 `checkCertificate c = true` | 完整全局证书尚不存在，最终定理未闭合 |
| “缓存写错会证明假命题” | 成功候选仍经证书检查 | 写错通常导致漏解或证书被拒；不能绕开检查器 |
| “精确局面键等于高性能搜索完成” | 键只是基础设施 | 递归置换表和增量状态仍需实现和测量 |
| “一个具体 `example` 能代表所有同类棋形” | 无自由变量时只证明那张棋盘 | 需要带变量与前提的通用定理 |
| “Lean 证明通过，所以自然语言概念一定定义对了” | Lean 不知道人的原意 | 还需语义审查、模式冻结和反例测试 |
| “文档里写已完成就等于证明” | 文档可能过时或夸大 | 以实际 Lean 定理、无占位和成功构建为准 |

## 附录 C：源码导航表

| 想了解的问题 | 先找的文件 | 关键名字 |
| --- | --- | --- |
| 黑白玩家如何表示 | `Gomoku/Basic.lean` | `Player`, `Player.other` |
| 坐标为何不会越界 | `Gomoku/Basic.lean` | `Coord` |
| 落子怎样修改棋盘 | `Gomoku/Basic.lean` | `Board.place`, `place_same`, `place_other` |
| 怎样统计棋子和空格 | `Gomoku/Basic.lean` | `Board.count`, `emptyCount` |
| 怎样沿直线移动 | `Gomoku/Geometry.lean` | `Direction`, `step` |
| 五连如何定义 | `Gomoku/Geometry.lean` | `consecutive`, `hasRun`, `hasAtLeastFive` |
| 开放端和活三/四是什么 | `Gomoku/Geometry.lean` | `openEnd`, `straightOpenThree`, `straightOpenFour` |
| 断三和跳四有哪些模式 | `Gomoku/Geometry.lean` | `brokenOpenThree`, `jumpFour` |
| 最大连续段如何定义 | `Gomoku/Geometry.lean` | `MaximalRun` |
| 正式局面和开局是什么 | `Gomoku/Rules.lean` | `Position`, `Position.initial` |
| 终局和结果如何计算 | `Gomoku/Rules.lean` | `isTerminal`, `terminal`, `Outcome` |
| 合法落子和换手 | `Gomoku/Rules.lean` | `legalMove`, `play` |
| 可达局面如何生成 | `Gomoku/Rules.lean` | `Position.Reachable` |
| 轮次和棋子数不变量 | `Gomoku/Rules.lean` | `reachable_count_invariant` |
| 强制获胜的严格含义 | `Gomoku/Game.lean` | `ForceWin`, `CanForceWin` |
| 策略函数如何连接获胜树 | `Gomoku/Game.lean` | `StrategyRealizes`, `strategyRealizes_iff_canForceWin` |
| 立即胜着和威胁点 | `Gomoku/Tactics.lean` | `WinningMoves`, `WinningCells` |
| 活三怎样连接到四连 | `Gomoku/Tactics.lean` | `FourExtensionCells` |
| 双威胁怎样强制胜 | `Gomoku/Tactics.lean` | `HasDoubleThreat`, `doubleThreat_forces_win` |
| 单活四定理 | `Gomoku/Tactics.lean` | `straightOpenFour_black_immediate`, `singleOpenFour_forces_win_minimal` |
| 安全双活三接口 | `Gomoku/Tactics.lean` | `SafeDoubleOpenThree`, `safeDoubleOpenThree_forces_win` |
| 策略证书是什么 | `Gomoku/Certificate.lean` | `CertificateTree`, `CompactCertificate` |
| 检查器检查什么 | `Gomoku/Certificate.lean` | `checkNodeAt`, `checkCertificate` |
| 检查器为何可靠 | `Gomoku/Certificate.lean` | `compact_reify_at`, `compact_certificate_sound` |
| 如何生成小证书 | `Gomoku/Search.lean` | `immediateCertificateFor`, `twoPlyCertificateFor` |
| 有限深度搜索 | `Gomoku/Search.lean` | `searchCandidateTree`, `checkedDepthCertificateFor` |
| 快速成五判断 | `Gomoku/Search.lean` | `createsFiveFast`, `createsFiveFast_terminal_iff` |
| 局面键和缓存 | `Gomoku/Search.lean` | `PositionKey`, `SearchKey`, `SearchMemo` |
| 基础正例在哪里 | `Gomoku/Examples.lean` | 各个 `example` |
| 刁钻反例在哪里 | `Gomoku/Adversarial.lean` | 坏棋形和坏证书回归 |
| 项目完整说明 | `PROJECT_GUIDE.md` | 阶段、接口和验收 |
| 已知审计问题 | `AUDIT_REPORT.md` | A1–A8 |
| 三层成果总结 | `LOCAL_PROJECT_ACHIEVEMENTS.md` | 面向外行、内行和实现细节 |

## 附录 D：20 个独立检查问题

先不看答案，用自己的话回答。能稳定答对 16 个以上，基本已经能够参与规划和第一轮 PR 审查。

1. `Board` 与 `Position` 的信息差别是什么？
2. 为什么一个手工构造的 `Position` 不自动满足 `Reachable`？
3. `legalMove s c` 包含哪两个条件？
4. 为什么 `play` 可以作用于非法着法？使用它时怎样保证规则正确？
5. 六连在本项目中是否获胜？依据哪个定义？
6. 为什么贴边的连续三子可以是最大连续段，却不是直线活三？
7. `WinningMoves`、`WinningCells`、`FourExtensionCells` 的核心差别是什么？
8. “两个模式见证”为什么不一定代表“两个独立即胜点”？
9. `CanForceWin` 在我方回合和对方回合分别使用什么量词？
10. 一个 `Strategy` 为什么可能不是获胜策略？
11. `play_emptyCount_lt` 在策略等价性证明中起什么作用？
12. `reachable_count_invariant` 能否用于任意手工棋盘？为什么？
13. `safeDoubleOpenThree_forces_win` 是否已经证明纯几何双活三必胜？
14. 紧凑证书的对手节点为什么必须列出全部合法应手？
15. 子索引严格大于父索引怎样排除证书循环？
16. `compact_certificate_sound` 已经证明了什么条件结论？还缺什么才能得到最终定理？
17. `createsFiveFast_terminal_iff` 需要哪两个关键前提？
18. 搜索返回 `none` 能否证明目标玩家没有胜法？
19. 当前 `SearchMemo` 为什么还不等于完整高性能置换表？
20. 审查一个新局部战术 PR 时，你至少要看哪些内容？

## 附录 E：检查问题参考答案

1. `Board` 只有各格内容；`Position` 还记录当前轮到谁。两者都不自动包含可达历史证明。
2. `Position` 可任意拼出；`Reachable` 必须从空盘黑先经合法步骤归纳产生，排除了错误历史。
3. 父局面未终局，目标格为空。
4. 它是方便组合的总函数。正式定理在前提中携带 `legalMove s c`，或由证书检查器验证合法性。
5. 获胜。`hasAtLeastFive` 定义为存在一个长度 5 的连续窗口，更长连中也含这样的窗口。
6. `MaximalRun` 只要求两端没有同色子；活三要求两端都是棋盘内空格。边界不是空格。
7. `WinningMoves` 看当前轮次和正式合法终局；`WinningCells` 不看轮次，只看空格假放后成五；`FourExtensionCells` 只要求假放后至少成四。
8. 同一真实威胁可能被不同起点/方向重复表示，而且活三见证对应下一步成四，不是立即成五点。
9. 我方回合存在一枚成功着法；对方回合对所有合法应手都必须有成功子证明。
10. `Strategy` 的类型只保证在相关局面返回合法棋；是否获胜由 `StrategyRealizes` 另行表达。
11. 它提供自然数严格下降尺度，使沿棋局递归构造策略的证明终止。
12. 不能。它明确以 `Reachable s` 为前提，并对该可达性证明归纳。
13. 没有。`SafeDoubleOpenThree` 已包含对手每个防守后仍 `CanForceWin` 的强条件；纯几何到安全性的桥仍未完成。
14. 少一枚应手就只证明了对手在已列选择中无法阻止，不是“无论怎样走”。
15. 有限数组中索引沿边只能严格增加，任何有限循环若回到原点便要求索引既大于自身，矛盾。
16. 它证明：任意证书若全局检查为真，则初始空盘黑棋 `CanForceWin`。还缺一份真实完整证书以及其检查为真的证明/计算闭环。
17. 当前轮到目标玩家，且着法合法。二者保证父局面无既有胜利、落子不覆盖，并把局部窗口判定连接到正式终局。
18. 不能。它可能表示深度为零、某分支在当前预算内未找到，或搜索器能力不足。
19. 目前主要是外层命中/未命中适配；递归子调用还不会持续传递、插入和复用更新后的表。
20. 至少看冻结定义、变量/前提/结论、轮次与合法性、对手全分支、见证唯一性、正反例、通用定理、可信边界、构建结果和剩余限制。

## 附录 F：你真正接手项目的判定标准

不要求你能从零重写项目。只要能独立做到下面这些，就已经可以担任项目规划者和第一层审查者：

1. 不看说明，用五分钟向外行解释当前成果与最终缺口；
2. 打开任意定理，圈出变量、前提、结论和隐含定义；
3. 对“这个棋形必胜”追问轮次、终局、合法性和对手全部应手；
4. 能运行 `lake build` 和单文件检查，并读出第一条实际错误；
5. 能区分通用定理、具体例子、接口、优化和最终目标；
6. 能按模板给队友分配一个范围明确的任务；
7. 能按清单审查 PR，并要求补充一个真正有针对性的反例；
8. 能准确说出为什么现在还没有 `initial_black_wins`。

达到这一步以后，再根据你最感兴趣的方向深入：偏数学可学习归纳类型、有限博弈和战术充分条件；偏工程可学习 Lean 数据结构、可执行判定、搜索和证书生成。你不需要同时成为五子棋大师、Lean 专家和高性能程序员，项目分层本来就是为了让不同能力的人在共同的严格接口上协作。

## 结语：现在应该怎样理解这个项目

目前项目不是“已经算出黑棋必胜”，也不是“只有一些五子棋定义”。它已经完成了从规则、几何、合法历史、强制获胜语义到可检查策略证书的一条可信骨架，并对若干局部棋形和搜索小闭环给出了真实 Lean 结果。它还主动保存了反例，说明哪些直觉不能被直接升级为定理。

下一阶段最重要的两件事是：继续把中等难度战术变成前提清楚的通用定理；把候选搜索扩展成能够产生更深、更大证书的工程系统。最终 15×15 黑棋必胜仍需要真实全局策略证书。只要团队始终让所有成功结果经过现有检查器，并准确汇报条件和范围，搜索器、启发式和外部实现都可以持续改进而不损害已经建立的可信基础。
